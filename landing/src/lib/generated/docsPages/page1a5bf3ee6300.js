export const pageContent = {
  "html": "<h1>테스트 단위 + Gate 운영</h1>\n<p>테스트의 기준은 &quot;지금 통과하는가&quot;가 아니라 &quot;다음 변경이 이 동작과 경계를 깨면 자동으로 fail 하는가&quot;다. 새 테스트는 어떤 실패 표면을 막는지 분명해야 한다.</p>\n<h2>단위</h2>\n<ul>\n<li><strong>Gate</strong>: 사람이 실행하고 CI가 호출하는 이름 붙은 검증 단위. source of truth는 <code>tests/run.py</code>다.</li>\n<li><strong>Suite</strong>: gate 안에서 실행되는 pytest, npm, cargo 같은 도구별 묶음.</li>\n<li><strong>Targeted test</strong>: 특정 계약을 빠르게 확인하는 파일/케이스 단위. 예: <code>teacher-eval</code>.</li>\n<li><strong>Contract fixture</strong>: tool sequence, trace payload, schema처럼 결과 모양을 고정하는 입력/출력 자료.</li>\n</ul>\n<h2>명령</h2>\n<pre><code class=\"language-bash\">uv run python -X utf8 tests/run.py list\nuv run python -X utf8 tests/run.py preflight\nuv run python -X utf8 tests/run.py quality-cycle\nuv run python -X utf8 tests/run.py gate backend\nuv run python -X utf8 tests/run.py gate teacher-eval\nuv run python -X utf8 tests/run.py gate teacher-e2e\nuv run python -X utf8 tests/run.py gate assistant-workloop-contract\nuv run python -X utf8 tests/run.py gate ai-live-smoke\nuv run python -X utf8 tests/run.py gate editor-runtime-preflight\nuv run python -X utf8 tests/run.py gate learning-system-readiness\nuv run python -X utf8 tests/run.py gate learning-goal-audit\nuv run python -X utf8 tests/run.py gate dogfood-alpha-audit\nuv run python -X utf8 tests/run.py gate product-quality-audit\nuv run python -X utf8 tests/run.py gate diagnostic-summary-contract\nuv run python -X utf8 tests/run.py gate install-launcher-smoke\nuv run python -X utf8 tests/run.py gate runtime-recovery-contract\nuv run python -X utf8 tests/run.py gate runtime-recovery-browser\nuv run python -X utf8 tests/run.py gate curriculum-quality-matrix\nuv run python -X utf8 tests/run.py gate onboarding-browser\nuv run python -X utf8 tests/run.py gate frontend-performance-budget\nuv run python -X utf8 tests/run.py gate learning-card-contract\nuv run python -X utf8 tests/run.py gate learning-card-browser\nuv run python -X utf8 tests/run.py gate provider-settings-browser\nuv run python -X utf8 tests/run.py gate editor-build\nuv run python -X utf8 tests/run.py gate landing-build\nuv run python -X utf8 tests/run.py gate launcher-check\nuv run python -X utf8 tests/run.py gate launcher-test\n</code></pre>\n<p>직접 <code>pytest tests/ -v</code>를 금지하지는 않는다. 다만 PR 전 확인, CI, 세션 종료 검증은 gate 이름으로 남긴다. <code>quality-cycle</code>은 “서비스 출시 라벨”이 아니라 제품이 잘 만들어졌는지 보는 묶음 실행 단위다.</p>\n<h2>실행 격리</h2>\n<p><code>tests/run.py</code>는 로컬/CI gate 실행 시 도구가 반드시 만드는 실행 작업공간만 저장소 안의 <code>output/test-runner/&lt;gate&gt;/</code> 아래로 고정한다. 제품 소스와 원래 build 설정은 건드리지 않고, 사용자 홈의 uv cache, OS temp, 기존 launcher <code>target</code> lock, 외부 <code>npx</code> fetch 상태가 gate 결과를 흔들지 않게 한다.</p>\n<ul>\n<li><code>uv run</code> 명령은 runner 안에서 <code>uv --no-cache run</code>으로 실행한다. gate runner는 uv cache를 만들거나 재사용하지 않는다.</li>\n<li>pytest suite는 cache provider를 끄고 <code>--basetemp output/test-runner/&lt;gate&gt;/pytest</code>를 자동으로 붙인다.</li>\n<li>cargo suite는 <code>--target-dir output/test-runner/&lt;gate&gt;/cargo-target</code>를 자동으로 붙여 기존 <code>target</code> lock과 충돌하지 않는다.</li>\n<li><code>TMP</code>, <code>TEMP</code>, <code>TMPDIR</code>은 도구 실행 중 필요한 scratch 용도로만 <code>output/test-runner/&lt;gate&gt;/scratch</code>를 가리킨다.</li>\n<li>브라우저 verifier는 <code>tempfile.mkdtemp</code>로 OS temp를 직접 만들지 않는다. <code>repoLocalPlaywrightWorkspace</code>를 통해 gate runner의 scratch를 쓰고, 직접 실행 시에도 <code>output/test-runner/&lt;verifier&gt;/scratch/playwright</code> 아래에서만 Playwright daemon/session 파일을 만든다. <code>PLAYWRIGHT_DAEMON_SESSION_DIR</code>와 <code>PLAYWRIGHT_SERVER_REGISTRY</code>는 wrapper가 이 workspace로 덮어쓴다.</li>\n<li><code>output/test-runner</code>는 disposable 실행 작업공간이며 제품 SSOT나 커밋 대상이 아니다.</li>\n</ul>\n<h2>Gate 목록</h2>\n<table>\n<thead>\n<tr>\n<th>Gate</th>\n<th>Tier</th>\n<th>역할</th>\n</tr>\n</thead>\n<tbody><tr>\n<td><code>docs</code></td>\n<td>fast</td>\n<td>운영 문서 포인터, gate 정의, CI 연결 상태를 확인한다.</td>\n</tr>\n<tr>\n<td><code>backend</code></td>\n<td>fast</td>\n<td>Python backend 전체 테스트를 실행한다.</td>\n</tr>\n<tr>\n<td><code>teacher-eval</code></td>\n<td>fast</td>\n<td>teacher tool policy, trace, golden eval 계약을 빠르게 확인한다.</td>\n</tr>\n<tr>\n<td><code>teacher-e2e</code></td>\n<td>fast</td>\n<td>scripted provider loop, provider error workloop, tool policy, 실제 curriculum YAML handler를 통과하는 golden e2e harness와 9점 기준 score를 실행한다.</td>\n</tr>\n<tr>\n<td><code>assistant-workloop-contract</code></td>\n<td>fast</td>\n<td>assistant workloop/trace UI state가 작업 전 확인 질문, provider 오류, tool detail을 보존하는지 확인한다.</td>\n</tr>\n<tr>\n<td><code>ai-live-smoke</code></td>\n<td>fast</td>\n<td>실제 provider credential이 있을 때 provider 응답, OAuth 상태, live tool loop smoke를 확인한다.</td>\n</tr>\n<tr>\n<td><code>editor-runtime-preflight</code></td>\n<td>fast</td>\n<td>editor 직접 실행 경로가 패키지 확인, uv 설치, 셀 실행 순서를 지키는지 확인한다.</td>\n</tr>\n<tr>\n<td><code>learning-system-readiness</code></td>\n<td>fast</td>\n<td>학습 YAML, 섹션 카드, teacher loop, workloop, gate SSOT의 readiness score를 확인한다.</td>\n</tr>\n<tr>\n<td><code>learning-goal-audit</code></td>\n<td>surface</td>\n<td>목표 완료 전 docs, product quality audit, readiness, backend, landing build를 묶어 확인한다.</td>\n</tr>\n<tr>\n<td><code>dogfood-alpha-audit</code></td>\n<td>surface</td>\n<td>사용자 플로우 audit으로 provider 연결, 질문, clarification, YAML 생성, 학습카드 렌더링, 실습 셀 입력, 셀 실행, 피드백, 실패 복구의 증거를 확인한다.</td>\n</tr>\n<tr>\n<td><code>product-quality-audit</code></td>\n<td>surface</td>\n<td>제품 품질 기준과 새 내구성 gate wiring을 확인한다.</td>\n</tr>\n<tr>\n<td><code>service-readiness-audit</code></td>\n<td>surface</td>\n<td>기존 자동화와 문서 링크를 위한 <code>product-quality-audit</code> 호환 alias다.</td>\n</tr>\n<tr>\n<td><code>diagnostic-summary-contract</code></td>\n<td>fast</td>\n<td>local diagnostic summary가 provider/runtime/package/frontend 실패 범주와 secret redaction 계약을 지키는지 확인한다.</td>\n</tr>\n<tr>\n<td><code>install-launcher-smoke</code></td>\n<td>release</td>\n<td>launcher doctor, health check, rollback, exact artifact 설치 경계와 <code>cargo check</code>를 확인한다.</td>\n</tr>\n<tr>\n<td><code>runtime-recovery-contract</code></td>\n<td>fast</td>\n<td>runtime worker crash, package preflight, uv 설치 실패, cell 실행 실패 복구 계약을 확인한다.</td>\n</tr>\n<tr>\n<td><code>runtime-recovery-browser</code></td>\n<td>surface</td>\n<td>브라우저에서 package install 실패가 셀 근처 복구 UX로 보이고 cell-call로 번지지 않는지 확인한다.</td>\n</tr>\n<tr>\n<td><code>curriculum-quality-matrix</code></td>\n<td>fast</td>\n<td>Python 기초, 파일 처리, 데이터 분석, 시각화, 웹 자동화 structured YAML이 섹션 카드 계약으로 materialize되는지 확인한다.</td>\n</tr>\n<tr>\n<td><code>onboarding-browser</code></td>\n<td>surface</td>\n<td>브라우저에서 첫 화면 fallback, Provider 연결 행동, provider 연결 후 실제 응답 상태를 확인한다.</td>\n</tr>\n<tr>\n<td><code>frontend-performance-budget</code></td>\n<td>surface</td>\n<td>editor build 후 chunk 분리와 JS/CSS asset size budget을 확인한다.</td>\n</tr>\n<tr>\n<td><code>learning-card-contract</code></td>\n<td>surface</td>\n<td>structured section card marker 계약과 editor build를 확인한다.</td>\n</tr>\n<tr>\n<td><code>learning-card-browser</code></td>\n<td>surface</td>\n<td>Playwright CLI로 lesson overview와 structured section card의 desktop/mobile 렌더링을 확인한다.</td>\n</tr>\n<tr>\n<td><code>provider-settings-browser</code></td>\n<td>surface</td>\n<td>Playwright CLI로 provider 설정 sheet의 fallback, OAuth login/status polling, 선택, 응답 검증, 실패 안내 렌더링을 확인한다.</td>\n</tr>\n<tr>\n<td><code>editor-build</code></td>\n<td>surface</td>\n<td>제품 editor surface의 TypeScript/Vite build를 확인한다.</td>\n</tr>\n<tr>\n<td><code>landing-build</code></td>\n<td>surface</td>\n<td>문서/landing surface의 static build와 docs content bundle split을 확인한다.</td>\n</tr>\n<tr>\n<td><code>launcher-check</code></td>\n<td>release</td>\n<td>launcher Rust crate의 type/build 계약을 확인한다.</td>\n</tr>\n<tr>\n<td><code>launcher-test</code></td>\n<td>release</td>\n<td>launcher Rust crate 테스트를 직렬 실행한다.</td>\n</tr>\n</tbody></table>\n<p><code>preflight</code>는 로컬 기본 확인이며 현재 <code>docs</code>와 <code>backend</code>를 실행한다. <code>backend</code>가 전체 pytest를 포함하므로 <code>teacher-eval</code>과 <code>teacher-e2e</code>는 빠른 집중 확인용으로 둔다.\n<code>quality-cycle</code>은 제품이 잘 만들어졌는지 보는 반복 검증 단위다. 순서는 <code>docs</code> → <code>backend</code> → <code>learning-system-readiness</code> → <code>dogfood-alpha-audit</code> → <code>product-quality-audit</code> → <code>diagnostic-summary-contract</code> → <code>ai-live-smoke</code> → <code>provider-settings-browser</code> → <code>install-launcher-smoke</code> → <code>runtime-recovery-contract</code> → <code>runtime-recovery-browser</code> → <code>curriculum-quality-matrix</code> → <code>onboarding-browser</code> → <code>frontend-performance-budget</code> → <code>landing-build</code> → <code>launcher-test</code>다. 이 명령은 완료 선언을 대신하지 않고, provider, 학습, runtime, 설치/런처, 온보딩, 프론트 성능이 한 사이클에서 함께 버티는지 확인한다. 묶음 실행이 끝나면 runner는 통과한 gate 수, soft failure 수, gate별 duration summary, 현재 <code>gitHead</code>, <code>startedAt</code>/<code>completedAt</code>, 그리고 gate별 artifact freshness를 <code>output/test-runner/quality-cycle/sequence-summary.json</code>에 남긴다. <code>ai-live-smoke</code>처럼 report를 쓰는 gate는 summary 안에 artifact path, fresh 여부, <code>payloadGitHead</code>, <code>gitHeadMatches</code>, <code>payloadStatus</code>가 함께 들어가야 하며, report의 git head가 sequence head와 맞지 않으면 artifact failure로 sequence를 실패시킨다. credential missing exit code 2는 <code>softFailure: true</code>와 <code>softFailureCount</code>로 기록하고 이후 gate를 계속 실행한다. 실제 provider 실패 exit code 1은 soft 처리하지 않고 sequence를 중단해야 한다. 이 summary는 제품 SSOT가 아니라 사람이 읽는 완료 증거다.</p>\n<h2>추가 규칙</h2>\n<ul>\n<li>새 gate는 <code>tests/run.py</code>, 이 문서, CI 중 필요한 위치를 함께 갱신한다.</li>\n<li>새 pytest 파일은 가능한 한 제품/도메인 경계를 드러내는 이름을 쓴다.</li>\n<li><code>ai-live-smoke</code>는 opt-in gate다. credential/token이 없으면 skip하지 않고 <code>live credential missing</code>을 JSON으로 보고한다. <code>CODARO_AI_LIVE_PROVIDERS=oauth-chatgpt,openai,ollama,custom</code>처럼 matrix를 명시하면 provider별 <code>passed</code>/<code>failed</code>/<code>credentialMissing</code> summary를 남긴다. credential missing과 provider exception은 <code>diagnostic.code</code>/<code>diagnostic.action</code>을 포함해 다시 로그인, API 키 입력, Base URL 입력, 네트워크 점검, OAuth 호환성 점검을 구분한다. live tool loop는 실제 provider 응답에서 <code>write-curriculum-yaml</code>과 <code>packages-check → cell-call</code> exact sequence가 발생하는지 보고, provider가 보낸 YAML을 실제 materializer로 변환해 <code>contractGapCount=0</code>, section/snippet/exercise cell 신호를 확인한다. 실행 결과는 <code>output/test-runner/ai-live-smoke/live-smoke-report.json</code>에 provider/model, case별 latency, diagnostic action, tool sequence, <code>workloopReadable</code>/<code>workloopLabels</code>/<code>workloopSamples</code>, tuning signal, <code>gitHead</code>, <code>startedAt</code>, <code>completedAt</code>, <code>durationMs</code>로 남긴다. 이 gate는 CI required가 아니며, credential missing exit code 2만 quality-cycle soft status로 기록한다. 실제 provider/OAuth/네트워크 실패 exit code 1은 hard failure로 남겨 기본 CI 안정성과 live provider 품질 판단을 분리한다.</li>\n<li>provider 설정 UI 변경은 <code>provider-settings-browser</code>로 실제 브라우저에서 연결 전 fallback, OAuth authorize/status polling의 실패/성공, 저장된 provider 선택 후 실제 응답 상태, OAuth 호환성/네트워크/base URL 실패 안내가 보이는지 확인한다. 이 gate는 stub provider API를 쓰므로 secret이나 실제 token을 저장소에 남기지 않는다.</li>\n<li>teacher/tool 변경은 최소한 tool sequence, policy violation, workloop label/detail, structured YAML contract, provider loop result signal 중 변경 표면 하나를 고정한다.</li>\n<li>provider loop 변경은 가능한 한 실제 scripted provider run으로 <code>packages-check</code> → <code>packages-install</code> → <code>cell-call</code>의 정확한 순서와 결과 필드(<code>missing</code>, <code>success</code>, <code>passed</code>)를 함께 검증한다. <code>packages-check</code> 실패 뒤 provider가 <code>cell-call</code>을 요청하는 negative golden도 executor 호출을 차단하고 <code>dependency-preflight-required</code> policy result를 provider에게 돌려줘야 한다. 다음 provider 호출에 직전 <code>role: tool</code> 결과 메시지가 들어갔는지도 확인한다. golden case가 요구하는 exact sequence에 불필요한 tool call이 끼거나 provider가 tool result를 보지 못하면 실패해야 한다. 큰 tool result는 provider message에서 bounded JSON으로 줄이되 현재 turn payload/trace의 full result는 보존해야 한다. streaming native tool loop 변경은 tool result 이후 다음 provider 호출 실패가 <code>error</code> event와 <code>trace.workloop</code>의 <code>provider 오류</code> row로 남는지도 고정한다.</li>\n<li>turn-state durability는 <code>teacher-eval</code>/<code>teacher-e2e</code>에서 고정한다. 이전 tool result가 다음 turn의 tool policy state를 통과시키면 실패해야 한다. 이전 turn의 <code>role: tool</code> 결과는 conversation history로 남아도 되고 재현에 필요하지만, 새 turn의 정책 상태를 만족시키는 근거가 되면 안 된다. 예를 들어 직전 <code>packages-check</code> 결과가 준비됨이어도 새 turn에서 provider가 바로 <code>cell-call</code>을 요청하면 executor 호출 없이 <code>dependency-preflight-required</code>로 막혀야 한다.</li>\n<li>editor runtime 실행 변경은 <code>editor-runtime-preflight</code>로 세션 패키지 확인, 누락 패키지 uv 설치, kernel 실행 순서가 지켜지는지 확인한다.</li>\n<li>provider loop, clarification, curriculum materializer를 함께 건드린 변경은 <code>teacher-e2e</code>로 실제 turn payload와 teacher golden e2e score를 확인한다.</li>\n<li>workloop/trace 표시 변경은 <code>assistant-workloop-contract</code>로 clarification 작업 기준, provider 오류 detail+error, packages-check/install/cell-call 표시 문장과 패키지 설치 result detail(<code>installer</code>, <code>environment</code>, <code>durationMs</code>, <code>skipped</code>)을 함께 확인한다.</li>\n<li>clarification gate 변경은 실제 provider 호출 없이 멈추는 golden provider run을 검증한다. <code>toolSequence</code>가 비어 있고, 질문 수 1-3개와 작업 기준 key, workloop label이 빠지면 실패해야 한다. 이어지는 <code>진행</code> 또는 짧은 조건 답변 턴은 <code>pendingClarification.assumptions</code>를 provider prompt의 <code>[Clarification plan]</code>으로 주입하고 한 번 소비하는지 확인한다. 반대로 <code>취소</code>, <code>새로</code>, <code>다른 주제</code> 같은 새 요청과 이미 구체적인 새 학습 요청에는 stale pending이 섞이지 않고 비워져야 한다.</li>\n<li>curriculum YAML/provider golden 변경은 실제 <code>write-curriculum-yaml</code> 핸들러를 통과한 document 변경을 검증한다. <code>loadedInEditor</code>, structured section card flow, document runtime packages, <code>intro.diagram.runtime</code> detail, <code>sectionCount</code>/<code>exerciseCellCount</code>/<code>contractGapCount</code> result signal이 빠지면 실패해야 한다. 신규 structured YAML의 <code>contractGapCount</code>가 0이 아니면 teacher golden은 실패해야 한다.</li>\n<li>학습카드/YAML 변경은 backend materializer 테스트, <code>learning-card-contract</code>, 레이아웃 변경 시 <code>learning-card-browser</code>를 함께 확인한다. <code>learning-card-contract</code>는 섹션 카드 part, 직접 입력 editor, <code>student-practice</code> 입력 역할, 셀 도움 팝오버, 제목 중복 제거, 스니펫 복사 버튼, push TOC, <code>data-learning-section-contract-gaps</code> 경고 band, 라이브러리 패널 상태/진행 marker를 고정한다. 또한 셀 도움은 해당 셀 안의 팝오버로 남아야 하고, Codaro 표면은 브랜드 아바타를 쓰며 로봇/봇 framing과 hover-only 도움 버튼으로 되돌아가면 실패해야 한다. <code>learning-card-browser</code>는 손으로 만든 fixture가 아니라 실제 <code>yamlToDocument</code> 산출물을 검증하고, 그 산출물의 렌더링 필드를 브라우저에 주입해야 한다. overview diagram은 YAML의 <code>intro.diagram.runtime</code> 문구가 화면의 runtime node로 렌더링되는지도 확인하고, 불완전한 structured section의 계약 gap 경고와 package panel이 desktop/mobile 카드 안에서 보이는지도 확인한다. desktop/mobile 모두에서 가로 overflow, 카드/overview 밖으로 탈출한 텍스트/버튼, 버튼 텍스트 overflow, control overlap도 visual integrity로 확인한다.</li>\n<li>목표 완료를 말하기 전에는 <code>learning-system-readiness</code>가 최소 9점을 증명해야 한다. 이 gate는 완료 선언을 대체하지 않고, YAML 계약, 카드 UI, clarification, uv 패키지 정책, editor runtime preflight, provider 오류 workloop, frontend workloop, golden eval/e2e, 운영 SSOT 증거가 현재 저장소에 남아 있는지 확인한다. 또한 <code>teacher-eval</code>, <code>teacher-e2e</code>, <code>assistant-workloop-contract</code>, <code>editor-runtime-preflight</code>, <code>learning-card-contract</code>, <code>learning-card-browser</code>를 실제로 실행하는 blocking probe가 실패하면 점수와 무관하게 실패해야 한다.</li>\n<li>목표를 닫기 전 최종 검증은 <code>learning-goal-audit</code>로 남긴다. 이 gate는 docs 정합성, dogfood alpha 사용자 플로우 audit, <code>product-quality-audit</code>, 명시 요구사항 audit, <code>learning-system-readiness</code>, 전체 backend, landing build를 한 번에 실행해 &quot;9점 readiness는 통과했지만 제품 빌드/문서/첫 사용자 완주 증거가 따로 깨진&quot; 상태를 막는다. 명시 요구사항 audit은 <code>quality-cycle</code>의 제품 품질 gate 묶음과 <code>generated-docs-freshness</code>까지 evidence로 요구하며, <code>score</code>, <code>maxScore</code>, <code>minimumScore</code>, <code>requiredScore</code>, <code>requirementFailures</code>를 남긴다. <code>minimumScore</code> 이상이어도 <code>requirementFailures</code>가 하나라도 있으면 실패한다. teacher/provider loop 자체의 산출물은 <code>score</code>, <code>maxScore</code>, <code>minimumScore</code>를 포함해야 하며 <code>minimumScore</code>는 9.0이다.</li>\n<li><code>dogfood-alpha-audit</code>는 첫 실행부터 provider 연결, 질문, clarification, YAML 생성, 학습카드 렌더링, 실습 셀 입력, 셀 실행, 피드백, 실패 복구까지 9단계가 문서와 코드 gate로 연결되어 있는지 확인한다. 제품 품질 판단은 이 audit과 live provider credential이 있는 환경의 <code>ai-live-smoke</code> 결과가 나온 뒤에만 한다.</li>\n<li><code>product-quality-audit</code>는 제품 품질 wiring audit이다. 이 audit은 단독으로 제품 완성을 증명하지 않고, <code>docs</code>, <code>backend</code>, <code>learning-system-readiness</code>, <code>dogfood-alpha-audit</code>, <code>diagnostic-summary-contract</code>, <code>ai-live-smoke</code>, <code>provider-settings-browser</code>, <code>install-launcher-smoke</code>, <code>runtime-recovery-contract</code>, <code>runtime-recovery-browser</code>, <code>curriculum-quality-matrix</code>, <code>onboarding-browser</code>, <code>frontend-performance-budget</code>, <code>landing-build</code>, <code>launcher-test</code>가 runner와 문서에 연결되어 있고 각 gate가 실제 실패 표면을 보는지 확인한다. <code>service-readiness-audit</code>는 이전 이름을 참조하는 자동화를 위한 호환 alias로만 둔다.</li>\n<li><code>diagnostic-summary-contract</code>는 local diagnostic summary의 최소 제품 계약이다. <code>/api/system/diagnostics</code>가 provider 연결 상태, uv/project <code>.venv</code>, runtime status, editor build 산출물을 provider/runtime/package/frontend category/count/action으로 분리하며, editor bootstrap이 이 summary를 시작 진단 안내로 보여주고, <code>token</code>, <code>apiKey</code>, <code>secret</code>, <code>Authorization</code>, OAuth access/refresh token, <code>sk-...</code> 값이 summary/detail/metadata에 남지 않는지 확인한다.</li>\n<li><code>install-launcher-smoke</code>는 launcher doctor, active/last-known-good/crash/rollback state, backend health timeout, exact wheel/sha256 packaging 경계를 본다. launcher 작업에서 이 gate를 통과하지 못하면 사용자 설치/실행/복구 경로가 제품 품질 판단에 올라갈 수 없다.</li>\n<li><code>runtime-recovery-contract</code>는 backend runtime 테스트, editor runtime preflight, workloop copy를 묶어 worker crash, package delay/failure, cell execution failure가 한 오류로 뭉개지지 않는지 확인한다. <code>runtime-recovery-browser</code>는 이 계약이 실제 learning surface에서 셀 근처 문구로 보이는지 확인한다.</li>\n<li><code>curriculum-quality-matrix</code>는 pandas 하나가 아니라 Python 기초, 파일 처리, 데이터 분석, 시각화, 웹 자동화 대표 주제를 실제 <code>yamlToDocument</code>로 materialize한다. <code>contractGapCount</code>가 0이 아니거나 섹션 흐름이 <code>section → explanation → snippet → exercise → check</code>를 벗어나면 실패한다.</li>\n<li><code>onboarding-browser</code>는 첫 화면에서 provider 연결 전 fallback이 명확하고 provider 연결 후 실제 응답 사용 상태가 분명한지 본다. 이 gate는 product surface 기준이며 landing page 상태를 대체하지 않는다.</li>\n<li><code>frontend-performance-budget</code>는 <code>editor/vite.config.ts</code>의 chunk split과 build output을 함께 본다. 큰 bundle 경고를 baseline 없이 방치하지 않고, 가장 큰 JS chunk와 전체 JS/CSS 크기를 수치로 남긴다.</li>\n<li><code>landing-build</code>는 공개 문서 surface가 generated docs 본문 HTML을 nav chunk에 싣지 않는지도 확인한다. <code>docsNav.js</code>는 metadata와 <code>contentModule</code>만 담고, 각 문서 본문은 <code>landing/src/lib/generated/docsPages/page*.js</code>로 분리되어 slug route에서 동적 로딩되어야 한다. <code>docs/skills</code> 핵심 SSOT 문구가 generated docs에 반영되지 않은 stale 상태도 실패로 본다.</li>\n<li>기존 부채를 새 테스트로 한 번에 해결하지 못하면 별도 baseline 또는 명시적 TODO 문서로 분리한다.</li>\n<li>CI YAML은 세부 명령을 소유하지 않고 <code>tests/run.py gate &lt;name&gt;</code>만 호출한다.</li>\n</ul>\n",
  "text": "테스트 단위 + Gate 운영 테스트의 기준은 \"지금 통과하는가\"가 아니라 \"다음 변경이 이 동작과 경계를 깨면 자동으로 fail 하는가\"다. 새 테스트는 어떤 실패 표면을 막는지 분명해야 한다. 단위 Gate : 사람이 실행하고 CI가 호출하는 이름 붙은 검증 단위. source of truth는 다. Suite : gate 안에서 실행되는 pytest, npm, cargo 같은 도구별 묶음. Targeted test : 특정 계약을 빠르게 확인하는 파일/케이스 단위. 예: . Contract fixture : tool sequence, trace payload, schema처럼 결과 모양을 고정하는 입력/출력 자료. 명령 직접 를 금지하지는 않는다. 다만 PR 전 확인, CI, 세션 종료 검증은 gate 이름으로 남긴다. 은 “서비스 출시 라벨”이 아니라 제품이 잘 만들어졌는지 보는 묶음 실행 단위다. 실행 격리 는 로컬/CI gate 실행 시 도구가 반드시 만드는 실행 작업공간만 저장소 안의 아래로 고정한다. 제품 소스와 원래 build 설정은 건드리지 않고, 사용자 홈의 uv cache, OS temp, 기존 launcher lock, 외부 fetch 상태가 gate 결과를 흔들지 않게 한다. 명령은 runner 안에서 으로 실행한다. gate runner는 uv cache를 만들거나 재사용하지 않는다. pytest suite는 cache provider를 끄고 를 자동으로 붙인다. cargo suite는 를 자동으로 붙여 기존 lock과 충돌하지 않는다. , , 은 도구 실행 중 필요한 scratch 용도로만 를 가리킨다. 브라우저 verifier는 로 OS temp를 직접 만들지 않는다. 를 통해 gate runner의 scratch를 쓰고, 직접 실행 시에도 아래에서만 Playwright daemon/session 파일을 만든다. 와 는 wrapper가 이 workspace로 덮어쓴다. 는 disposable 실행 작업공간이며 제품 SSOT나 커밋 대상이 아니다. Gate 목록 | Gate | Tier | 역할 | | | | | | | fast | 운영 문서 포인터, gate 정의, CI 연결 상태를 확인한다. | | | fast | Python backend 전체 테스트를 실행한다. | | | fast | teacher tool policy, trace, golden eval 계약을 빠르게 확인한다. | | | fast | scripted provider loop, provider error workloop, tool policy, 실제 curriculum YAML handler를 통과하는 golden e2e harness와 9점 기준 score를 실행한다. | | | fast | assistant workloop/trace UI state가 작업 전 확인 질문, provider 오류, tool detail을 보존하는지 확인한다. | | | fast | 실제 provider credential이 있을 때 provider 응답, OAuth 상태, live tool loop smoke를 확인한다. | | | fast | editor 직접 실행 경로가 패키지 확인, uv 설치, 셀 실행 순서를 지키는지 확인한다. | | | fast | 학습 YAML, 섹션 카드, teacher loop, workloop, gate SSOT의 readiness score를 확인한다. | | | surface | 목표 완료 전 docs, product quality audit, readiness, backend, landing build를 묶어 확인한다. | | | surface | 사용자 플로우 audit으로 provider 연결, 질문, clarification, YAML 생성, 학습카드 렌더링, 실습 셀 입력, 셀 실행, 피드백, 실패 복구의 증거를 확인한다. | | | surface | 제품 품질 기준과 새 내구성 gate wiring을 확인한다. | | | surface | 기존 자동화와 문서 링크를 위한 호환 alias다. | | | fast | local diagnostic summary가 provider/runtime/package/frontend 실패 범주와 secret redaction 계약을 지키는지 확인한다. | | | release | launcher doctor, health check, rollback, exact artifact 설치 경계와 를 확인한다. | | | fast | runtime worker crash, package preflight, uv 설치 실패, cell 실행 실패 복구 계약을 확인한다. | | | surface | 브라우저에서 package install 실패가 셀 근처 복구 UX로 보이고 cell call로 번지지 않는지 확인한다. | | | fast | Python 기초, 파일 처리, 데이터 분석, 시각화, 웹 자동화 structured YAML이 섹션 카드 계약으로 materialize되는지 확인한다. | | | surface | 브라우저에서 첫 화면 fallback, Provider 연결 행동, provider 연결 후 실제 응답 상태를 확인한다. | | | surface | editor build 후 chunk 분리와 JS/CSS asset size budget을 확인한다. | | | surface | structured section card marker 계약과 editor build를 확인한다. | | | surface | Playwright CLI로 lesson overview와 structured section card의 desktop/mobile 렌더링을 확인한다. | | | surface | Playwright CLI로 provider 설정 sheet의 fallback, OAuth login/status polling, 선택, 응답 검증, 실패 안내 렌더링을 확인한다. | | | surface | 제품 editor surface의 TypeScript/Vite build를 확인한다. | | | surface | 문서/landing surface의 static build와 docs content bundle split을 확인한다. | | | release | launcher Rust crate의 type/build 계약을 확인한다. | | | release | launcher Rust crate 테스트를 직렬 실행한다. | 는 로컬 기본 확인이며 현재 와 를 실행한다. 가 전체 pytest를 포함하므로 과 는 빠른 집중 확인용으로 둔다. 은 제품이 잘 만들어졌는지 보는 반복 검증 단위다. 순서는 → → → → → → → → → → → → → → → 다. 이 명령은 완료 선언을 대신하지 않고, provider, 학습, runtime, 설치/런처, 온보딩, 프론트 성능이 한 사이클에서 함께 버티는지 확인한다. 묶음 실행이 끝나면 runner는 통과한 gate 수, soft failure 수, gate별 duration summary, 현재 , / , 그리고 gate별 artifact freshness를 에 남긴다. 처럼 report를 쓰는 gate는 summary 안에 artifact path, fresh 여부, , , 가 함께 들어가야 하며, report의 git head가 sequence head와 맞지 않으면 artifact failure로 sequence를 실패시킨다. credential missing exit code 2는 와 로 기록하고 이후 gate를 계속 실행한다. 실제 provider 실패 exit code 1은 soft 처리하지 않고 sequence를 중단해야 한다. 이 summary는 제품 SSOT가 아니라 사람이 읽는 완료 증거다. 추가 규칙 새 gate는 , 이 문서, CI 중 필요한 위치를 함께 갱신한다. 새 pytest 파일은 가능한 한 제품/도메인 경계를 드러내는 이름을 쓴다. 는 opt in gate다. credential/token이 없으면 skip하지 않고 을 JSON으로 보고한다. 처럼 matrix를 명시하면 provider별 / / summary를 남긴다. credential missing과 provider exception은 / 을 포함해 다시 로그인, API 키 입력, Base URL 입력, 네트워크 점검, OAuth 호환성 점검을 구분한다. live tool loop는 실제 provider 응답에서 과 exact sequence가 발생하는지 보고, provider가 보낸 YAML을 실제 materializer로 변환해 , section/snippet/exercise cell 신호를 확인한다. 실행 결과는 에 provider/model, case별 latency, diagnostic action, tool sequence, / / , tuning signal, , , , 로 남긴다. 이 gate는 CI required가 아니며, credential missing exit code 2만 quality cycle soft status로 기록한다. 실제 provider/OAuth/네트워크 실패 exit code 1은 hard failure로 남겨 기본 CI 안정성과 live provider 품질 판단을 분리한다. provider 설정 UI 변경은 로 실제 브라우저에서 연결 전 fallback, OAuth authorize/status polling의 실패/성공, 저장된 provider 선택 후 실제 응답 상태, OAuth 호환성/네트워크/base URL 실패 안내가 보이는지 확인한다. 이 gate는 stub provider API를 쓰므로 secret이나 실제 token을 저장소에 남기지 않는다. teacher/tool 변경은 최소한 tool sequence, policy violation, workloop label/detail, structured YAML contract, provider loop result signal 중 변경 표면 하나를 고정한다. provider loop 변경은 가능한 한 실제 scripted provider run으로 → → 의 정확한 순서와 결과 필드( , , )를 함께 검증한다. 실패 뒤 provider가 을 요청하는 negative golden도 executor 호출을 차단하고 policy result를 provider에게 돌려줘야 한다. 다음 provider 호출에 직전 결과 메시지가 들어갔는지도 확인한다. golden case가 요구하는 exact sequence에 불필요한 tool call이 끼거나 provider가 tool result를 보지 못하면 실패해야 한다. 큰 tool result는 provider message에서 bounded JSON으로 줄이되 현재 turn payload/trace의 full result는 보존해야 한다. streaming native tool loop 변경은 tool result 이후 다음 provider 호출 실패가 event와 의 row로 남는지도 고정한다. turn state durability는 / 에서 고정한다. 이전 tool result가 다음 turn의 tool policy state를 통과시키면 실패해야 한다. 이전 turn의 결과는 conversation history로 남아도 되고 재현에 필요하지만, 새 turn의 정책 상태를 만족시키는 근거가 되면 안 된다. 예를 들어 직전 결과가 준비됨이어도 새 turn에서 provider가 바로 을 요청하면 executor 호출 없이 로 막혀야 한다. editor runtime 실행 변경은 로 세션 패키지 확인, 누락 패키지 uv 설치, kernel 실행 순서가 지켜지는지 확인한다. provider loop, clarification, curriculum materializer를 함께 건드린 변경은 로 실제 turn payload와 teacher golden e2e score를 확인한다. workloop/trace 표시 변경은 로 clarification 작업 기준, provider 오류 detail+error, packages check/install/cell call 표시 문장과 패키지 설치 result detail( , , , )을 함께 확인한다. clarification gate 변경은 실제 provider 호출 없이 멈추는 golden provider run을 검증한다. 가 비어 있고, 질문 수 1 3개와 작업 기준 key, workloop label이 빠지면 실패해야 한다. 이어지는 또는 짧은 조건 답변 턴은 를 provider prompt의 으로 주입하고 한 번 소비하는지 확인한다. 반대로 , , 같은 새 요청과 이미 구체적인 새 학습 요청에는 stale pending이 섞이지 않고 비워져야 한다. curriculum YAML/provider golden 변경은 실제 핸들러를 통과한 document 변경을 검증한다. , structured section card flow, document runtime packages, detail, / / result signal이 빠지면 실패해야 한다. 신규 structured YAML의 가 0이 아니면 teacher golden은 실패해야 한다. 학습카드/YAML 변경은 backend materializer 테스트, , 레이아웃 변경 시 를 함께 확인한다. 는 섹션 카드 part, 직접 입력 editor, 입력 역할, 셀 도움 팝오버, 제목 중복 제거, 스니펫 복사 버튼, push TOC, 경고 band, 라이브러리 패널 상태/진행 marker를 고정한다. 또한 셀 도움은 해당 셀 안의 팝오버로 남아야 하고, Codaro 표면은 브랜드 아바타를 쓰며 로봇/봇 framing과 hover only 도움 버튼으로 되돌아가면 실패해야 한다. 는 손으로 만든 fixture가 아니라 실제 산출물을 검증하고, 그 산출물의 렌더링 필드를 브라우저에 주입해야 한다. overview diagram은 YAML의 문구가 화면의 runtime node로 렌더링되는지도 확인하고, 불완전한 structured section의 계약 gap 경고와 package panel이 desktop/mobile 카드 안에서 보이는지도 확인한다. desktop/mobile 모두에서 가로 overflow, 카드/overview 밖으로 탈출한 텍스트/버튼, 버튼 텍스트 overflow, control overlap도 visual integrity로 확인한다. 목표 완료를 말하기 전에는 가 최소 9점을 증명해야 한다. 이 gate는 완료 선언을 대체하지 않고, YAML 계약, 카드 UI, clarification, uv 패키지 정책, editor runtime preflight, provider 오류 workloop, frontend workloop, golden eval/e2e, 운영 SSOT 증거가 현재 저장소에 남아 있는지 확인한다. 또한 , , , , , 를 실제로 실행하는 blocking probe가 실패하면 점수와 무관하게 실패해야 한다. 목표를 닫기 전 최종 검증은 로 남긴다. 이 gate는 docs 정합성, dogfood alpha 사용자 플로우 audit, , 명시 요구사항 audit, , 전체 backend, landing build를 한 번에 실행해 \"9점 readiness는 통과했지만 제품 빌드/문서/첫 사용자 완주 증거가 따로 깨진\" 상태를 막는다. 명시 요구사항 audit은 의 제품 품질 gate 묶음과 까지 evidence로 요구하며, , , , , 를 남긴다. 이상이어도 가 하나라도 있으면 실패한다. teacher/provider loop 자체의 산출물은 , , 를 포함해야 하며 는 9.0이다. 는 첫 실행부터 provider 연결, 질문, clarification, YAML 생성, 학습카드 렌더링, 실습 셀 입력, 셀 실행, 피드백, 실패 복구까지 9단계가 문서와 코드 gate로 연결되어 있는지 확인한다. 제품 품질 판단은 이 audit과 live provider credential이 있는 환경의 결과가 나온 뒤에만 한다. 는 제품 품질 wiring audit이다. 이 audit은 단독으로 제품 완성을 증명하지 않고, , , , , , , , , , , , , , , 가 runner와 문서에 연결되어 있고 각 gate가 실제 실패 표면을 보는지 확인한다. 는 이전 이름을 참조하는 자동화를 위한 호환 alias로만 둔다. 는 local diagnostic summary의 최소 제품 계약이다. 가 provider 연결 상태, uv/project , runtime status, editor build 산출물을 provider/runtime/package/frontend category/count/action으로 분리하며, editor bootstrap이 이 summary를 시작 진단 안내로 보여주고, , , , , OAuth access/refresh token, 값이 summary/detail/metadata에 남지 않는지 확인한다. 는 launcher doctor, active/last known good/crash/rollback state, backend health timeout, exact wheel/sha256 packaging 경계를 본다. launcher 작업에서 이 gate를 통과하지 못하면 사용자 설치/실행/복구 경로가 제품 품질 판단에 올라갈 수 없다. 는 backend runtime 테스트, editor runtime preflight, workloop copy를 묶어 worker crash, package delay/failure, cell execution failure가 한 오류로 뭉개지지 않는지 확인한다. 는 이 계약이 실제 learning surface에서 셀 근처 문구로 보이는지 확인한다. 는 pandas 하나가 아니라 Python 기초, 파일 처리, 데이터 분석, 시각화, 웹 자동화 대표 주제를 실제 로 materialize한다. 가 0이 아니거나 섹션 흐름이 를 벗어나면 실패한다. 는 첫 화면에서 provider 연결 전 fallback이 명확하고 provider 연결 후 실제 응답 사용 상태가 분명한지 본다. 이 gate는 product surface 기준이며 landing page 상태를 대체하지 않는다. 는 의 chunk split과 build output을 함께 본다. 큰 bundle 경고를 baseline 없이 방치하지 않고, 가장 큰 JS chunk와 전체 JS/CSS 크기를 수치로 남긴다. 는 공개 문서 surface가 generated docs 본문 HTML을 nav chunk에 싣지 않는지도 확인한다. 는 metadata와 만 담고, 각 문서 본문은 로 분리되어 slug route에서 동적 로딩되어야 한다. 핵심 SSOT 문구가 generated docs에 반영되지 않은 stale 상태도 실패로 본다. 기존 부채를 새 테스트로 한 번에 해결하지 못하면 별도 baseline 또는 명시적 TODO 문서로 분리한다. CI YAML은 세부 명령을 소유하지 않고 만 호출한다."
};
