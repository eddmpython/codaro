export const pageContent = {
  "html": "<h1>Live Provider Ops</h1>\n<p>Codaro의 기본 gate는 deterministic scripted provider를 사용한다. 실제 provider는 네트워크, 계정, 토큰, 외부 endpoint 영향을 받으므로 CI 기본 경로에 넣지 않는다. 대신 사람이 명시적으로 실행하는 opt-in live gate로 제품 경로를 검증한다.</p>\n<h2>Provider 범위</h2>\n<table>\n<thead>\n<tr>\n<th>Provider</th>\n<th>인증</th>\n<th>용도</th>\n<th>실패 경계</th>\n</tr>\n</thead>\n<tbody><tr>\n<td><code>oauth-chatgpt</code></td>\n<td>ChatGPT OAuth token</td>\n<td>구독 기반 실제 응답과 native tool loop 확인</td>\n<td>token 없음, 만료, refresh 실패, endpoint/header/SSE 변경</td>\n</tr>\n<tr>\n<td><code>openai</code></td>\n<td>API key 또는 <code>OPENAI_API_KEY</code></td>\n<td>OpenAI API key 기반 실제 응답 확인</td>\n<td>key 없음, 권한/과금/모델 오류, 네트워크</td>\n</tr>\n<tr>\n<td><code>ollama</code></td>\n<td>로컬 서버</td>\n<td>오프라인/로컬 모델 smoke</td>\n<td>서버 미실행, 모델 미설치</td>\n</tr>\n<tr>\n<td><code>custom</code></td>\n<td>OpenAI-compatible base URL + key</td>\n<td>사내/로컬 호환 endpoint</td>\n<td>base URL/key 없음, schema 비호환</td>\n</tr>\n</tbody></table>\n<p>실제 token과 API key는 저장소에 남기지 않는다. gate output도 token 값을 출력하지 않는다.</p>\n<p><code>oauth-chatgpt</code>는 현재 tool call event는 native function call로 받을 수 있지만, Codaro가 쓰는 experimental endpoint는 <code>previous_response_id</code> 기반 tool-result continuation을 지원하지 않는다. 따라서 tool 결과는 <code>[tool_result id=...]</code> user text bridge로 되돌려 보내고, <code>packages-check</code> 같은 lifecycle 결과에는 <code>codaroNextRequiredTool</code>, <code>codaroProviderInstruction</code>, <code>codaroToolPolicy.nextRequiredTool</code>을 함께 넣는다. 이렇게 해야 실제 provider가 <code>packages-check</code>를 반복 호출하지 않고 <code>packages-install</code> 또는 <code>cell-call</code>로 넘어간다. 이 경계는 <code>tests/teacher/testAiProvider.py</code>, <code>tests/teacher/testTeacherArchitecture.py</code>, <code>ai-live-smoke</code>가 같이 고정한다.</p>\n<h2>OAuth 흐름</h2>\n<ol>\n<li>Provider 설정에서 OAuth login을 시작한다.</li>\n<li><code>/api/oauth/authorize</code>가 auth URL과 state를 만들고 callback server를 연다.</li>\n<li>Browser login 후 <code>/auth/callback</code>에서 state를 검증한다.</li>\n<li>code exchange가 성공하면 token을 secret store에 저장하고 active provider를 <code>oauth-chatgpt</code>로 갱신한다.</li>\n<li>질문 turn은 <code>prepareTeacherRuntimeTurn</code> → provider → tool loop → trace/workloop 순서로 실행된다.</li>\n</ol>\n<p><code>tests/teacher/testAiProvider.py</code>는 callback 성공 후 같은 profile/secret store로 teacher 질문을 준비해 <code>oauth-chatgpt</code> provider config가 생성되고 응답 payload가 반환되는 경로를 검증한다. live credential을 쓰는 외부 smoke와 별개로, OAuth login → callback → token 저장 → profile active provider 갱신 → 질문 준비/응답 경로의 제품 내부 연결은 이 테스트가 담당한다.</p>\n<p>실패는 raw exception으로 끝내지 않는다.</p>\n<ul>\n<li>token 없음: <code>live credential missing</code> 또는 provider 로그인 필요.</li>\n<li>state mismatch: 보안 검증 실패로 표시.</li>\n<li>OAuth consent 거부: 권한 문제로 표시.</li>\n<li>refresh 만료/재사용/폐기: 다시 로그인 필요.</li>\n<li>network timeout/connection: 네트워크 문제.</li>\n<li>endpoint/header/SSE 변경: OAuth provider compatibility 점검 필요.</li>\n</ul>\n<p>HTTP/stream/UI 경계에는 <code>code</code>, <code>message</code>, <code>action</code>, <code>provider</code>, <code>detail</code>, <code>recoverable</code>을 가진 진단 payload를 넘긴다. 기본 화면은 <code>message</code>만 보여주고, raw detail은 trace나 확장 진단에서만 본다. editor는 <code>connect-provider</code>, <code>relogin-provider</code>, <code>restart-login</code>, <code>configure-api-key</code>, <code>configure-base-url</code>, <code>check-permission</code>, <code>check-network</code>, <code>check-provider</code>, <code>check-provider-compatibility</code>, <code>retry-later</code> action을 구분해 설정 열기/재로그인/키 입력/base URL 입력/권한 확인/네트워크 점검/provider 상태 확인/호환성 점검/나중에 재시도로 안내한다.</p>\n<p>질문 실행 중 발생하는 token refresh 실패도 callback 실패와 같은 기준으로 분류한다. refresh token이 만료/재사용/폐기되면 <code>relogin-provider</code>, refresh 요청이나 refresh 후 재시도 요청이 네트워크에서 실패하면 <code>check-network</code>, OAuth client/header/endpoint/SSE 변화가 의심되면 <code>check-provider-compatibility</code>를 남긴다. 이 구분은 provider 설정 UI뿐 아니라 chat/stream error와 workloop trace에서도 유지되어야 한다.</p>\n<p>Chat/stream error 경계는 문자열 추측보다 <code>diagnostic.action</code>을 우선한다. <code>connect-provider</code>, <code>relogin-provider</code>, <code>restart-login</code>, <code>configure-api-key</code>, <code>configure-base-url</code>은 사용자를 Provider 설정으로 보낼 수 있지만, <code>check-network</code>나 <code>check-provider-compatibility</code>는 OAuth라는 단어가 있어도 로그인 필요로 바꾸지 않는다. <code>turn-error</code> workloop에는 provider, diagnostic code, diagnostic action을 함께 남겨 나중에 실제 질문 실패를 재현할 수 있어야 한다.</p>\n<p>제품 질문 경로도 같은 계약을 따른다. <code>/api/ai/chat</code>은 provider 오류를 raw exception으로 올리지 않고 사용자 메시지와 trace/workloop로 반환한다. <code>/api/ai/chat/stream</code>은 <code>error</code> SSE event에 같은 <code>diagnostic</code> payload를 싣는다. OAuth refresh 만료는 두 경로 모두 <code>provider_relogin_required</code> + <code>relogin-provider</code>로 보여야 하며, 사용자에게 보이는 answer/error에는 <code>refresh_token</code> 같은 raw detail을 넣지 않는다.</p>\n<p>Provider 설정의 검증은 <code>probe=availability</code>와 <code>probe=response</code>를 구분한다. 목록/상태 확인은 availability probe로 충분하지만, 로그인/저장/선택 직후와 사용자가 누르는 “응답 검증”은 response probe를 써서 짧은 실제 응답까지 확인한다. <code>openai</code>는 저장된 key 또는 <code>OPENAI_API_KEY</code>, <code>custom</code>은 base URL과 key, <code>ollama</code>는 로컬 서버와 모델 응답, <code>oauth-chatgpt</code>는 저장 token과 live endpoint 응답을 기준으로 실패 메시지를 갈라야 한다.</p>\n<p>Provider 설정 UI는 검증 결과를 일회성 toast로만 처리하지 않는다. 로그인/저장/선택/응답 검증에서 나온 마지막 <code>ProviderValidationSnapshot</code>을 provider별로 보존하고 카드 안에 표시한다. OAuth login을 시작하면 callback/status polling 동안 카드가 “브라우저 로그인 대기” 상태로 바뀌어 사용자가 열린 로그인 탭을 완료해야 한다는 점을 보여준다. 연결 전에는 “기본 안내 모드”로 실제 응답을 쓰지 않는다는 점을 보여주고, 검증 통과 후에는 “실제 응답 사용 중” 또는 “응답 검증 완료”를 표시한다. 실패 시에는 <code>diagnostic.action</code>을 사람이 이해할 수 있는 조치로 바꾼다. 예: <code>connect-provider</code>/<code>relogin-provider</code>는 다시 로그인, <code>restart-login</code>은 로그인 다시 시작, <code>configure-api-key</code>는 API 키 입력, <code>configure-base-url</code>은 Base URL 입력, <code>check-permission</code>은 권한 문제, <code>check-network</code>는 네트워크 문제, <code>check-provider</code>는 provider 상태 확인, <code>check-provider-compatibility</code>는 OAuth 호환성 점검, <code>retry-later</code>는 잠시 후 재시도다. raw detail은 기본 UI에 노출하지 않는다. assistant/chat 경계도 diagnostic action을 우선한다. <code>OAuth</code>라는 단어가 들어 있더라도 action이 <code>check-provider-compatibility</code>, <code>check-permission</code>, <code>check-network</code>면 로그인 필요로 오분류하지 않는다.</p>\n<p><code>provider-settings-browser</code> gate는 실제 editor surface를 브라우저로 열고 stub provider API를 붙여 이 UI 계약을 확인한다. 검증 범위는 연결 전 fallback 표시, OAuth authorize/status polling의 pending 상태, state mismatch 실패, 권한 거부 실패, OAuth login 성공 후 실제 응답 상태, 저장된 provider 선택 후 실제 응답 상태, OAuth 호환성 실패, 네트워크 실패, base URL 실패 안내, 데스크톱/모바일 overflow 방지다. stub을 쓰므로 실제 token/API key는 필요하지 않으며 저장소에 남지 않는다.</p>\n<h2>Live Smoke Gate</h2>\n<p>명령:</p>\n<pre><code class=\"language-bash\">uv run python -X utf8 tests/run.py gate ai-live-smoke\n</code></pre>\n<p>환경 선택:</p>\n<pre><code class=\"language-bash\">set CODARO_AI_LIVE_PROVIDER=oauth-chatgpt\nset CODARO_AI_LIVE_MODEL=gpt-5.4\n</code></pre>\n<p><code>CODARO_AI_LIVE_PROVIDER</code>가 없으면 현재 profile의 default provider를 쓴다. credential이 없으면 gate는 <code>live credential missing</code>을 JSON으로 출력하고 실패 코드로 끝난다. 이것은 skip이 아니다. live credential이 없다는 사실을 명확히 보고하는 상태다.</p>\n<p>여러 provider를 한 번에 확인할 때는 matrix 환경을 명시한다.</p>\n<pre><code class=\"language-bash\">set CODARO_AI_LIVE_PROVIDERS=oauth-chatgpt,openai,ollama,custom\nuv run python -X utf8 tests/run.py gate ai-live-smoke\n</code></pre>\n<p><code>CODARO_AI_LIVE_PROVIDERS=all</code>은 공개 provider 전체를 뜻한다. matrix 모드는 provider별 결과를 <code>providers[]</code>에 남기고 <code>summary.passed</code>, <code>summary.failed</code>, <code>summary.credentialMissing</code>를 함께 출력한다.</p>\n<ul>\n<li>전체 통과: exit <code>0</code>, status <code>passed</code>.</li>\n<li>provider 실행 실패: exit <code>1</code>, status <code>failed</code>.</li>\n<li>하나라도 credential이 없고 실행 실패는 없을 때: exit <code>2</code>, status <code>live credential missing</code> 또는 <code>partial credential missing</code>.</li>\n</ul>\n<p>matrix에서 missing credential은 silent skip이 아니다. 사용자가 여러 provider를 명시했으면 어떤 provider가 준비되지 않았는지 보고서에 남겨야 한다.</p>\n<p>현재 live smoke는 다음을 확인한다.</p>\n<ul>\n<li>provider availability.</li>\n<li>짧은 일반 질문 응답.</li>\n<li>teacher system prompt를 통과한 질문 응답.</li>\n<li>모호한 학습 요청이 provider 호출 전에 deterministic clarification gate에서 멈추는지.</li>\n<li>구체적 학습 요청이 실제 provider 응답에서 <code>resolve-learning-goal → search-curricula → compose-master-plan</code> 추천·조합을 먼저 거치고, compose 결과가 gap을 보고할 때만 <code>packages-check → write-curriculum-yaml</code>로 이어지는지. provider가 보낸 YAML은 실제 materializer에서 섹션/스니펫/실습 셀과 <code>contractGapCount=0</code>으로 변환되어야 한다.</li>\n<li>YAML 작성 뒤 결과 확인을 위한 <code>read-cells</code> 같은 읽기 전용 inspection tool은 허용하지만, 쓰기/실행 tool이 끼면 실패로 본다.</li>\n<li>실행 요청이 실제 provider 응답에서 <code>packages-check</code> 이후 <code>cell-call</code> tool call로 이어지는지.</li>\n<li>응답 payload에 provider/model, case latency, tool sequence, executor call summary가 남는지.</li>\n<li>live tool loop와 cell-call loop의 workloop는 count만 남기지 않는다. report에는 <code>workloopReadable</code>, <code>workloopLabels</code>, <code>workloopSamples</code>를 남겨 사용자가 본 진행 문장과 같은 label/detail 품질을 확인한다.</li>\n<li><code>oauth-chatgpt</code> text bridge가 unsupported <code>previous_response_id</code>를 보내지 않고, <code>codaroNextRequiredTool</code>/<code>codaroProviderInstruction</code>/<code>codaroToolPolicy</code> 힌트로 다음 tool을 명확히 전달하는지.</li>\n</ul>\n<p>실행 결과는 stdout만 믿지 않는다. gate는 성공, 실패, credential missing, matrix partial 상태 모두 <code>output/test-runner/ai-live-smoke/live-smoke-report.json</code>에 같은 payload를 남긴다. 이 파일에는 provider/model, case별 latency, diagnostic action, tool sequence, workloop readable samples, tuning signal, <code>gitHead</code>, <code>startedAt</code>, <code>completedAt</code>, <code>durationMs</code>를 넣되 token/API key 값은 넣지 않는다.</p>\n<h2>평가 기준</h2>\n<p>Live 응답은 deterministic하지 않으므로 exact text를 비교하지 않는다. 아래 구조와 행동을 본다.</p>\n<ul>\n<li>answer가 비어 있지 않다.</li>\n<li>teacher 답변이 과도하게 장황하지 않다.</li>\n<li>ambiguous learning request는 provider를 호출하지 않는다.</li>\n<li>concrete learning request는 답변만으로 끝나지 않고 <code>resolve-learning-goal → search-curricula → compose-master-plan</code> 추천·조합을 먼저 만족해야 한다. compose 결과가 gap을 보고할 때만 <code>packages-check → write-curriculum-yaml</code>이 이어지며, 그 YAML은 structured section card 계약을 통과해야 한다. 이후 <code>read-cells</code> 같은 읽기 전용 검증은 허용하되 불필요한 쓰기/실행 tool은 허용하지 않는다.</li>\n<li>executable cell request는 <code>packages-check → cell-call</code> exact sequence만 사용하고 policy violation 없이 executor까지 전달한다.</li>\n<li>OAuth ChatGPT의 실행 요청은 <code>[tool_result id=...]</code> bridge와 <code>codaroToolPolicy.nextRequiredTool=cell-call</code> 힌트를 거쳐도 같은 exact sequence를 만족해야 한다.</li>\n<li>tool loop가 실패하면 “prompt/tool schema tuning required” 같은 문자열만 남기지 않는다. live smoke payload의 <code>signals</code>에 <code>failureReason</code>, <code>tuningRequired</code>, <code>expectedTools</code>, <code>observedTools</code>, <code>answerPreview</code>, <code>tuningHints</code>를 남겨 prompt, tool schema, parser 중 어디를 조정할지 판단할 수 있어야 한다.</li>\n<li>tool 결과를 돌려준 뒤 다음 provider 호출이 <code>provider_network_error</code>로 끊긴 경우는 prompt/schema tuning 실패로 오분류하지 않는다. live smoke는 recoverable network case를 제한적으로 재시도하고, 통과하더라도 <code>attemptCount</code>, <code>networkRetryCount</code>, <code>previousNetworkFailures</code>, <code>recoveredAfterNetworkRetry</code>를 report에 남긴다.</li>\n<li>provider/model/latency/error가 live smoke report에 남는다.</li>\n</ul>\n<h2>운영 원칙</h2>\n<ul>\n<li><code>ai-live-smoke</code>는 CI required gate가 아니다.</li>\n<li>live credential이 없는 환경에서 기본 CI와 preflight를 깨지 않는다.</li>\n<li>provider endpoint가 변할 수 있는 <code>oauth-chatgpt</code>는 experimental live provider로 취급한다.</li>\n<li>live gate 실패는 scripted provider gate 실패와 의미가 다르다. 제품 구조가 깨졌는지, credential/네트워크 문제인지, 외부 provider behavior 변화인지 구분해서 본다.</li>\n<li><code>ai-live-smoke</code>의 credential missing과 provider exception은 <code>liveCredentialDiagnostic</code>/<code>liveProviderExceptionDiagnostic</code>을 거쳐 기존 provider diagnostic 계약으로 남긴다. payload에는 <code>diagnostic.code</code>, <code>diagnostic.action</code>, 사용자 메시지, recoverable 여부가 들어가야 하며, case <code>signals</code>에는 <code>diagnosticCode</code>와 <code>diagnosticAction</code>을 남긴다. 운영자는 raw exception 문자열보다 이 action을 먼저 보고 다시 로그인, API 키 입력, Base URL 입력, 네트워크 점검, OAuth 호환성 점검을 구분한다.</li>\n<li>실제 계정으로 tool loop를 돌릴 때도 gate executor는 side effect를 최소화한다. package install과 cell execution은 live smoke에서는 simulated result로 막고, provider의 tool call 의도와 trace 품질을 먼저 본다.</li>\n</ul>\n<h2>관련 파일</h2>\n<ul>\n<li><code>tests/teacher/verifyAiLiveSmoke.py</code></li>\n<li><code>tests/surface/verifyProviderSettingsPlaywright.py</code></li>\n<li><code>src/codaro/ai/providers/oauthChatgptProvider.py</code></li>\n<li><code>src/codaro/ai/oauthFlow.py</code></li>\n<li><code>src/codaro/ai/oauthToken.py</code></li>\n<li><code>src/codaro/ai/providerValidation.py</code></li>\n<li><code>src/codaro/ai/teacher/providerLoop.py</code></li>\n<li><code>tests/teacher/testAiProvider.py</code></li>\n<li><code>editor/src/lib/providerConnection.ts</code></li>\n<li><code>editor/src/components/assistant/providerSettingsSheet.tsx</code></li>\n<li><code>tests/surface/testProviderSettingsContract.py</code></li>\n</ul>\n",
  "text": "Live Provider Ops Codaro의 기본 gate는 deterministic scripted provider를 사용한다. 실제 provider는 네트워크, 계정, 토큰, 외부 endpoint 영향을 받으므로 CI 기본 경로에 넣지 않는다. 대신 사람이 명시적으로 실행하는 opt in live gate로 제품 경로를 검증한다. Provider 범위 | Provider | 인증 | 용도 | 실패 경계 | | | | | | | | ChatGPT OAuth token | 구독 기반 실제 응답과 native tool loop 확인 | token 없음, 만료, refresh 실패, endpoint/header/SSE 변경 | | | API key 또는 | OpenAI API key 기반 실제 응답 확인 | key 없음, 권한/과금/모델 오류, 네트워크 | | | 로컬 서버 | 오프라인/로컬 모델 smoke | 서버 미실행, 모델 미설치 | | | OpenAI compatible base URL + key | 사내/로컬 호환 endpoint | base URL/key 없음, schema 비호환 | 실제 token과 API key는 저장소에 남기지 않는다. gate output도 token 값을 출력하지 않는다. 는 현재 tool call event는 native function call로 받을 수 있지만, Codaro가 쓰는 experimental endpoint는 기반 tool result continuation을 지원하지 않는다. 따라서 tool 결과는 user text bridge로 되돌려 보내고, 같은 lifecycle 결과에는 , , 을 함께 넣는다. 이렇게 해야 실제 provider가 를 반복 호출하지 않고 또는 로 넘어간다. 이 경계는 , , 가 같이 고정한다. OAuth 흐름 1. Provider 설정에서 OAuth login을 시작한다. 2. 가 auth URL과 state를 만들고 callback server를 연다. 3. Browser login 후 에서 state를 검증한다. 4. code exchange가 성공하면 token을 secret store에 저장하고 active provider를 로 갱신한다. 5. 질문 turn은 → provider → tool loop → trace/workloop 순서로 실행된다. 는 callback 성공 후 같은 profile/secret store로 teacher 질문을 준비해 provider config가 생성되고 응답 payload가 반환되는 경로를 검증한다. live credential을 쓰는 외부 smoke와 별개로, OAuth login → callback → token 저장 → profile active provider 갱신 → 질문 준비/응답 경로의 제품 내부 연결은 이 테스트가 담당한다. 실패는 raw exception으로 끝내지 않는다. token 없음: 또는 provider 로그인 필요. state mismatch: 보안 검증 실패로 표시. OAuth consent 거부: 권한 문제로 표시. refresh 만료/재사용/폐기: 다시 로그인 필요. network timeout/connection: 네트워크 문제. endpoint/header/SSE 변경: OAuth provider compatibility 점검 필요. HTTP/stream/UI 경계에는 , , , , , 을 가진 진단 payload를 넘긴다. 기본 화면은 만 보여주고, raw detail은 trace나 확장 진단에서만 본다. editor는 , , , , , , , , , action을 구분해 설정 열기/재로그인/키 입력/base URL 입력/권한 확인/네트워크 점검/provider 상태 확인/호환성 점검/나중에 재시도로 안내한다. 질문 실행 중 발생하는 token refresh 실패도 callback 실패와 같은 기준으로 분류한다. refresh token이 만료/재사용/폐기되면 , refresh 요청이나 refresh 후 재시도 요청이 네트워크에서 실패하면 , OAuth client/header/endpoint/SSE 변화가 의심되면 를 남긴다. 이 구분은 provider 설정 UI뿐 아니라 chat/stream error와 workloop trace에서도 유지되어야 한다. Chat/stream error 경계는 문자열 추측보다 을 우선한다. , , , , 은 사용자를 Provider 설정으로 보낼 수 있지만, 나 는 OAuth라는 단어가 있어도 로그인 필요로 바꾸지 않는다. workloop에는 provider, diagnostic code, diagnostic action을 함께 남겨 나중에 실제 질문 실패를 재현할 수 있어야 한다. 제품 질문 경로도 같은 계약을 따른다. 은 provider 오류를 raw exception으로 올리지 않고 사용자 메시지와 trace/workloop로 반환한다. 은 SSE event에 같은 payload를 싣는다. OAuth refresh 만료는 두 경로 모두 + 로 보여야 하며, 사용자에게 보이는 answer/error에는 같은 raw detail을 넣지 않는다. Provider 설정의 검증은 와 를 구분한다. 목록/상태 확인은 availability probe로 충분하지만, 로그인/저장/선택 직후와 사용자가 누르는 “응답 검증”은 response probe를 써서 짧은 실제 응답까지 확인한다. 는 저장된 key 또는 , 은 base URL과 key, 는 로컬 서버와 모델 응답, 는 저장 token과 live endpoint 응답을 기준으로 실패 메시지를 갈라야 한다. Provider 설정 UI는 검증 결과를 일회성 toast로만 처리하지 않는다. 로그인/저장/선택/응답 검증에서 나온 마지막 을 provider별로 보존하고 카드 안에 표시한다. OAuth login을 시작하면 callback/status polling 동안 카드가 “브라우저 로그인 대기” 상태로 바뀌어 사용자가 열린 로그인 탭을 완료해야 한다는 점을 보여준다. 연결 전에는 “기본 안내 모드”로 실제 응답을 쓰지 않는다는 점을 보여주고, 검증 통과 후에는 “실제 응답 사용 중” 또는 “응답 검증 완료”를 표시한다. 실패 시에는 을 사람이 이해할 수 있는 조치로 바꾼다. 예: / 는 다시 로그인, 은 로그인 다시 시작, 는 API 키 입력, 은 Base URL 입력, 은 권한 문제, 는 네트워크 문제, 는 provider 상태 확인, 는 OAuth 호환성 점검, 는 잠시 후 재시도다. raw detail은 기본 UI에 노출하지 않는다. assistant/chat 경계도 diagnostic action을 우선한다. 라는 단어가 들어 있더라도 action이 , , 면 로그인 필요로 오분류하지 않는다. gate는 실제 editor surface를 브라우저로 열고 stub provider API를 붙여 이 UI 계약을 확인한다. 검증 범위는 연결 전 fallback 표시, OAuth authorize/status polling의 pending 상태, state mismatch 실패, 권한 거부 실패, OAuth login 성공 후 실제 응답 상태, 저장된 provider 선택 후 실제 응답 상태, OAuth 호환성 실패, 네트워크 실패, base URL 실패 안내, 데스크톱/모바일 overflow 방지다. stub을 쓰므로 실제 token/API key는 필요하지 않으며 저장소에 남지 않는다. Live Smoke Gate 명령: 환경 선택: 가 없으면 현재 profile의 default provider를 쓴다. credential이 없으면 gate는 을 JSON으로 출력하고 실패 코드로 끝난다. 이것은 skip이 아니다. live credential이 없다는 사실을 명확히 보고하는 상태다. 여러 provider를 한 번에 확인할 때는 matrix 환경을 명시한다. 은 공개 provider 전체를 뜻한다. matrix 모드는 provider별 결과를 에 남기고 , , 를 함께 출력한다. 전체 통과: exit , status . provider 실행 실패: exit , status . 하나라도 credential이 없고 실행 실패는 없을 때: exit , status 또는 . matrix에서 missing credential은 silent skip이 아니다. 사용자가 여러 provider를 명시했으면 어떤 provider가 준비되지 않았는지 보고서에 남겨야 한다. 현재 live smoke는 다음을 확인한다. provider availability. 짧은 일반 질문 응답. teacher system prompt를 통과한 질문 응답. 모호한 학습 요청이 provider 호출 전에 deterministic clarification gate에서 멈추는지. 구체적 학습 요청이 실제 provider 응답에서 추천·조합을 먼저 거치고, compose 결과가 gap을 보고할 때만 로 이어지는지. provider가 보낸 YAML은 실제 materializer에서 섹션/스니펫/실습 셀과 으로 변환되어야 한다. YAML 작성 뒤 결과 확인을 위한 같은 읽기 전용 inspection tool은 허용하지만, 쓰기/실행 tool이 끼면 실패로 본다. 실행 요청이 실제 provider 응답에서 이후 tool call로 이어지는지. 응답 payload에 provider/model, case latency, tool sequence, executor call summary가 남는지. live tool loop와 cell call loop의 workloop는 count만 남기지 않는다. report에는 , , 를 남겨 사용자가 본 진행 문장과 같은 label/detail 품질을 확인한다. text bridge가 unsupported 를 보내지 않고, / / 힌트로 다음 tool을 명확히 전달하는지. 실행 결과는 stdout만 믿지 않는다. gate는 성공, 실패, credential missing, matrix partial 상태 모두 에 같은 payload를 남긴다. 이 파일에는 provider/model, case별 latency, diagnostic action, tool sequence, workloop readable samples, tuning signal, , , , 를 넣되 token/API key 값은 넣지 않는다. 평가 기준 Live 응답은 deterministic하지 않으므로 exact text를 비교하지 않는다. 아래 구조와 행동을 본다. answer가 비어 있지 않다. teacher 답변이 과도하게 장황하지 않다. ambiguous learning request는 provider를 호출하지 않는다. concrete learning request는 답변만으로 끝나지 않고 추천·조합을 먼저 만족해야 한다. compose 결과가 gap을 보고할 때만 이 이어지며, 그 YAML은 structured section card 계약을 통과해야 한다. 이후 같은 읽기 전용 검증은 허용하되 불필요한 쓰기/실행 tool은 허용하지 않는다. executable cell request는 exact sequence만 사용하고 policy violation 없이 executor까지 전달한다. OAuth ChatGPT의 실행 요청은 bridge와 힌트를 거쳐도 같은 exact sequence를 만족해야 한다. tool loop가 실패하면 “prompt/tool schema tuning required” 같은 문자열만 남기지 않는다. live smoke payload의 에 , , , , , 를 남겨 prompt, tool schema, parser 중 어디를 조정할지 판단할 수 있어야 한다. tool 결과를 돌려준 뒤 다음 provider 호출이 로 끊긴 경우는 prompt/schema tuning 실패로 오분류하지 않는다. live smoke는 recoverable network case를 제한적으로 재시도하고, 통과하더라도 , , , 를 report에 남긴다. provider/model/latency/error가 live smoke report에 남는다. 운영 원칙 는 CI required gate가 아니다. live credential이 없는 환경에서 기본 CI와 preflight를 깨지 않는다. provider endpoint가 변할 수 있는 는 experimental live provider로 취급한다. live gate 실패는 scripted provider gate 실패와 의미가 다르다. 제품 구조가 깨졌는지, credential/네트워크 문제인지, 외부 provider behavior 변화인지 구분해서 본다. 의 credential missing과 provider exception은 / 을 거쳐 기존 provider diagnostic 계약으로 남긴다. payload에는 , , 사용자 메시지, recoverable 여부가 들어가야 하며, case 에는 와 을 남긴다. 운영자는 raw exception 문자열보다 이 action을 먼저 보고 다시 로그인, API 키 입력, Base URL 입력, 네트워크 점검, OAuth 호환성 점검을 구분한다. 실제 계정으로 tool loop를 돌릴 때도 gate executor는 side effect를 최소화한다. package install과 cell execution은 live smoke에서는 simulated result로 막고, provider의 tool call 의도와 trace 품질을 먼저 본다. 관련 파일"
};
