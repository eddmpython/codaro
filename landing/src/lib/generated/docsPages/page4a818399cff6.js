export const pageContent = {
  "html": "<h1>AttributeError 디버깅 패턴</h1>\n<p><code>AttributeError: &#39;...&#39; object has no attribute &#39;...&#39;</code> 는 객체에 그 이름의 속성/메서드가\n없다는 신호다.</p>\n<h2>1. 객체 타입과 사용 가능한 속성 확인</h2>\n<pre><code class=\"language-python\">print(type(obj).__name__)\nprint([attr for attr in dir(obj) if not attr.startswith(&#39;_&#39;)])\n</code></pre>\n<p>내가 가진 게 정말 <code>DataFrame</code> 인지 <code>Series</code> 인지 — 자주 헷갈리는 게 pandas 객체끼리의\n혼동이다. <code>df.head()</code> 는 DataFrame 에 있지만 <code>dict.head()</code> 는 없다.</p>\n<h2>2. None 객체 점검</h2>\n<p><code>None.something</code> 은 거의 모든 AttributeError 의 단골 원인. <code>func()</code> 가 <code>None</code> 을 반환하는\n경우 — 흔한 예: <code>sorted_list = items.sort()</code> (sort 는 in-place, None 반환).</p>\n<pre><code class=\"language-python\">result = some_call()\nprint(&#39;result:&#39;, result)  # None?\n</code></pre>\n<h2>3. 오타·메서드 이름</h2>\n<p><code>.upper()</code> vs <code>.uppercase()</code>, <code>.append()</code> vs <code>.add()</code>, <code>.split()</code> vs <code>.spilt()</code>. 잘\n모르면 <code>dir(obj)</code> 로 후보 출력 후 가장 가까운 이름을 골라본다.</p>\n<pre><code class=\"language-python\">import difflib\nprint(difflib.get_close_matches(&#39;upercase&#39;, dir(&#39;hello&#39;)))\n</code></pre>\n<p>핵심: AttributeError 는 <strong>타입 가정과 실제 타입의 차이</strong> 또는 <strong>None 흐름</strong> 문제. 객체\n타입을 먼저 출력하고, 거기서 사용 가능한 속성을 직접 본다.</p>\n",
  "text": "AttributeError 디버깅 패턴 는 객체에 그 이름의 속성/메서드가 없다는 신호다. 1. 객체 타입과 사용 가능한 속성 확인 내가 가진 게 정말 인지 인지 — 자주 헷갈리는 게 pandas 객체끼리의 혼동이다. 는 DataFrame 에 있지만 는 없다. 2. None 객체 점검 은 거의 모든 AttributeError 의 단골 원인. 가 을 반환하는 경우 — 흔한 예: (sort 는 in place, None 반환). 3. 오타·메서드 이름 vs , vs , vs . 잘 모르면 로 후보 출력 후 가장 가까운 이름을 골라본다. 핵심: AttributeError 는 타입 가정과 실제 타입의 차이 또는 None 흐름 문제. 객체 타입을 먼저 출력하고, 거기서 사용 가능한 속성을 직접 본다."
};
