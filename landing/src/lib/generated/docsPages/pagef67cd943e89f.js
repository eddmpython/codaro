export const pageContent = {
  "html": "<h1>실행 환경 규칙</h1>\n<ul>\n<li>PowerShell 메인 가상환경은 프로젝트 루트 <code>.venv</code>를 사용한다.</li>\n<li>WSL에서는 루트 <code>.venv</code>를 공유하지 않는다.</li>\n<li>WSL 전용 가상환경이 필요하면 <code>.venv-wsl</code>을 사용한다.</li>\n<li>Python 3.12 이상과 <code>uv</code>를 기준으로 한다.</li>\n<li>Node.js는 <code>landing/</code> 문서 표면과 <code>editor/</code> 제품 표면 작업에 필요하다.</li>\n</ul>\n<h1>실행 인코딩 규칙</h1>\n<ul>\n<li>Python 실행은 기본적으로 <code>uv run python -X utf8 ...</code> 형태를 사용한다.</li>\n<li><code>uv</code> 명령은 기본 캐시를 쓰는 기본 실행을 우선한다. <code>uv run ...</code>이 첫 선택이며, uv cache 권한 오류가 나와도 권한 상승을 묻지 말고 현재 세션 권한으로 즉시 재실행한다.</li>\n<li>Codex/agent 실행에서 <code>uv</code> 때문에 <code>sandbox_permissions=require_escalated</code> 같은 승인 요청을 사용하지 않는다. 승인이 막힌 환경에서는 승인 요청 자체가 실패 원인이며, <code>pip</code> 직접 실행이나 수동 venv 변경으로 우회하지 않는다.</li>\n<li>재실행 후에도 같은 uv cache 오류가 지속될 때만 원인을 기록하고, 필요하면 작업 단위에서 <code>UV_CACHE_DIR</code> 같은 우회를 명시적으로 남긴다.</li>\n<li>PowerShell에서 인코딩이 의심되면 실행 전에 아래를 적용한다.<ul>\n<li><code>[Console]::OutputEncoding = [System.Text.UTF8Encoding]::new()</code></li>\n</ul>\n</li>\n<li>파일 읽기/쓰기 명령은 가능한 한 UTF-8을 명시한다.</li>\n<li>모든 텍스트 파일은 UTF-8을 기본으로 유지한다.</li>\n</ul>\n<h1>로컬 산출물 위생</h1>\n<ul>\n<li>canonical root tree와 새 파일 위치 기준은 <code>docs/skills/architecture/repository-structure.md</code>를 따른다.</li>\n<li>저장소 루트는 항상 제품/운영 SSOT 파일만 보이는 상태를 유지한다. 새 실습 파일, 임시 데이터, 로그, preview 출력은 루트에 만들지 않는다.</li>\n<li>루트에 <code>.txt</code>, <code>.csv</code>, <code>.log</code>, <code>.pid</code>, <code>.tmp</code>, <code>.tsbuildinfo</code>, <code>.ipynb</code>, <code>.sqlite</code>, <code>.db</code>, <code>.parquet</code> 같은 로컬 산출물을 남기면 <code>root-clean</code> gate가 실패해야 한다.</li>\n<li>파일 입출력 예제와 커리큘럼 코드는 <code>open(&quot;data.txt&quot;)</code>, <code>Path(&quot;output.txt&quot;)</code>처럼 루트 상대 경로에 쓰지 않는다. <code>tempfile.TemporaryDirectory()</code>, <code>Path(tempfile.gettempdir()) / &quot;codaro_...&quot;</code>, 또는 gate runner가 제공하는 <code>output/test-runner/&lt;gate&gt;/scratch</code> 아래를 사용한다.</li>\n<li>프로젝트 루트는 임시 로그/스크린샷/캐시/세션 파일을 쓰는 장소가 아니다.</li>\n<li>백그라운드 서버를 띄울 때 <code>*.log</code>, <code>*.pid</code>, <code>.playwright-cli/</code>, <code>vite-dev*.log</code>, <code>tsconfig.tsbuildinfo</code>를 루트나 앱 루트에 만들지 않는다.</li>\n<li>stdout/stderr 리다이렉트가 필요하면 <code>$env:TEMP\\codaro\\...</code> 아래를 우선 사용한다.</li>\n<li>보존이 필요 없는 로컬 산출물은 명시 요청이 있을 때 삭제한다. 보존이 꼭 필요하면 <code>_backup/</code>, <code>_archive/</code>, <code>_reference/</code> 같은 백업성 루트에 숨기지 말고 정식 제품/문서 위치로 승격한다. 새 실행 산출물은 레포 밖 <code>$env:TEMP\\codaro\\...</code>를 우선 사용한다.</li>\n<li>Playwright CLI는 레포 루트에서 실행하지 않는다. 브라우저 산출물이 필요하면 임시 작업 폴더나 <code>$env:TEMP\\codaro\\...</code>를 사용한다.</li>\n<li>백그라운드 프로세스를 띄운 작업은 종료 전 반드시 프로세스를 정리하고, 루트에 <code>*.log</code>, <code>.playwright-cli/</code>, 실습 <code>.txt</code>/<code>.csv</code>가 남지 않았는지 <code>uv run python -X utf8 tests/run.py gate root-clean</code>으로 확인한다.</li>\n</ul>\n<h1>주요 명령</h1>\n<pre><code class=\"language-bash\">uv run codaro\nuv run codaro path.py\nuv run codaro export path.py --format codaro\nuv run codaro export path.py --format ipynb\nuv run python -X utf8 tests/run.py preflight\nuv run python -X utf8 tests/run.py list\n</code></pre>\n<h1>패키지 설치</h1>\n<ul>\n<li>사용자가 에디터에서 라이브러리를 설치해도 내부 경로는 <code>/api/packages/install</code> 또는 세션 패키지 capability를 거쳐 <code>uv pip ... --python .venv</code>로 실행한다.</li>\n<li>AI 교사 도구도 같은 경로를 사용한다. <code>packages-check</code>로 누락 여부를 확인한 뒤 <code>packages-install</code>을 호출한다.</li>\n<li>plain package가 이미 <code>.venv</code>에 설치되어 있으면 <code>uv pip install</code>을 다시 실행하지 않고 즉시 성공으로 보고한다. version specifier나 extras가 있으면 정확성을 위해 <code>uv</code> 경로를 탄다.</li>\n<li>설치 결과는 <code>installer: uv</code>, <code>environment: project .venv</code>, <code>durationMs</code>, <code>skipped</code>를 포함한다. 에디터와 teacher trace는 이 값을 사용해 &quot;uv로 준비 중/완료/이미 준비됨&quot;을 표시한다.</li>\n<li>설치 timeout은 긴 wheel 준비를 고려해 <code>INSTALL_TIMEOUT_SECONDS</code> 상수로 관리한다. timeout 변경은 <code>tests/runtime/testSystem.py</code>에서 함께 고정한다.</li>\n<li>직접 <code>pip install</code> 명령을 문서, 테스트, UI 안내에 추가하지 않는다.</li>\n</ul>\n<h1>프론트 표면 실행</h1>\n<ul>\n<li><code>editor/</code> — 실제 제품 UI 표면. React + shadcn/ui 기준.</li>\n<li><code>landing/</code> — GitHub Pages 문서와 블로그 표면.</li>\n<li>폐기된 이전 편집기는 현재 저장소의 실행 표면으로 보지 않는다.</li>\n</ul>\n<p>제품 프론트를 반복 개발할 때는 <code>editor/</code>를 기준으로 본다.</p>\n",
  "text": "실행 환경 규칙 PowerShell 메인 가상환경은 프로젝트 루트 를 사용한다. WSL에서는 루트 를 공유하지 않는다. WSL 전용 가상환경이 필요하면 을 사용한다. Python 3.12 이상과 를 기준으로 한다. Node.js는 문서 표면과 제품 표면 작업에 필요하다. 실행 인코딩 규칙 Python 실행은 기본적으로 형태를 사용한다. 명령은 기본 캐시를 쓰는 기본 실행을 우선한다. 이 첫 선택이며, uv cache 권한 오류가 나와도 권한 상승을 묻지 말고 현재 세션 권한으로 즉시 재실행한다. Codex/agent 실행에서 때문에 같은 승인 요청을 사용하지 않는다. 승인이 막힌 환경에서는 승인 요청 자체가 실패 원인이며, 직접 실행이나 수동 venv 변경으로 우회하지 않는다. 재실행 후에도 같은 uv cache 오류가 지속될 때만 원인을 기록하고, 필요하면 작업 단위에서 같은 우회를 명시적으로 남긴다. PowerShell에서 인코딩이 의심되면 실행 전에 아래를 적용한다. 파일 읽기/쓰기 명령은 가능한 한 UTF 8을 명시한다. 모든 텍스트 파일은 UTF 8을 기본으로 유지한다. 로컬 산출물 위생 canonical root tree와 새 파일 위치 기준은 를 따른다. 저장소 루트는 항상 제품/운영 SSOT 파일만 보이는 상태를 유지한다. 새 실습 파일, 임시 데이터, 로그, preview 출력은 루트에 만들지 않는다. 루트에 , , , , , , , , , 같은 로컬 산출물을 남기면 gate가 실패해야 한다. 파일 입출력 예제와 커리큘럼 코드는 , 처럼 루트 상대 경로에 쓰지 않는다. , , 또는 gate runner가 제공하는 아래를 사용한다. 프로젝트 루트는 임시 로그/스크린샷/캐시/세션 파일을 쓰는 장소가 아니다. 백그라운드 서버를 띄울 때 , , , , 를 루트나 앱 루트에 만들지 않는다. stdout/stderr 리다이렉트가 필요하면 아래를 우선 사용한다. 보존이 필요 없는 로컬 산출물은 명시 요청이 있을 때 삭제한다. 보존이 꼭 필요하면 , , 같은 백업성 루트에 숨기지 말고 정식 제품/문서 위치로 승격한다. 새 실행 산출물은 레포 밖 를 우선 사용한다. Playwright CLI는 레포 루트에서 실행하지 않는다. 브라우저 산출물이 필요하면 임시 작업 폴더나 를 사용한다. 백그라운드 프로세스를 띄운 작업은 종료 전 반드시 프로세스를 정리하고, 루트에 , , 실습 / 가 남지 않았는지 으로 확인한다. 주요 명령 패키지 설치 사용자가 에디터에서 라이브러리를 설치해도 내부 경로는 또는 세션 패키지 capability를 거쳐 로 실행한다. AI 교사 도구도 같은 경로를 사용한다. 로 누락 여부를 확인한 뒤 을 호출한다. plain package가 이미 에 설치되어 있으면 을 다시 실행하지 않고 즉시 성공으로 보고한다. version specifier나 extras가 있으면 정확성을 위해 경로를 탄다. 설치 결과는 , , , 를 포함한다. 에디터와 teacher trace는 이 값을 사용해 \"uv로 준비 중/완료/이미 준비됨\"을 표시한다. 설치 timeout은 긴 wheel 준비를 고려해 상수로 관리한다. timeout 변경은 에서 함께 고정한다. 직접 명령을 문서, 테스트, UI 안내에 추가하지 않는다. 프론트 표면 실행 — 실제 제품 UI 표면. React + shadcn/ui 기준. — GitHub Pages 문서와 블로그 표면. 폐기된 이전 편집기는 현재 저장소의 실행 표면으로 보지 않는다. 제품 프론트를 반복 개발할 때는 를 기준으로 본다."
};
