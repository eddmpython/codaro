export const pageContent = {
  "html": "<h1>실행 환경 규칙</h1>\n<ul>\n<li>PowerShell 메인 가상환경은 프로젝트 루트 <code>.venv</code>를 사용한다.</li>\n<li>WSL에서는 루트 <code>.venv</code>를 공유하지 않는다.</li>\n<li>WSL 전용 가상환경이 필요하면 <code>.venv-wsl</code>을 사용한다.</li>\n<li>Python 3.12 이상과 <code>uv</code>를 기준으로 한다.</li>\n<li>Node.js는 <code>landing/</code> 문서 표면과 <code>editor/</code> 제품 표면 작업에 필요하다.</li>\n</ul>\n<h1>실행 인코딩 규칙</h1>\n<ul>\n<li>Python 실행은 기본적으로 <code>uv run python -X utf8 ...</code> 형태를 사용한다.</li>\n<li>PowerShell에서 인코딩이 의심되면 실행 전에 아래를 적용한다.<ul>\n<li><code>[Console]::OutputEncoding = [System.Text.UTF8Encoding]::new()</code></li>\n</ul>\n</li>\n<li>파일 읽기/쓰기 명령은 가능한 한 UTF-8을 명시한다.</li>\n<li>모든 텍스트 파일은 UTF-8을 기본으로 유지한다.</li>\n</ul>\n<h1>로컬 산출물 위생</h1>\n<ul>\n<li>프로젝트 루트는 임시 로그/스크린샷/캐시/세션 파일을 쓰는 장소가 아니다.</li>\n<li>백그라운드 서버를 띄울 때 <code>*.log</code>, <code>*.pid</code>, <code>.playwright-cli/</code>, <code>vite-dev*.log</code>, <code>tsconfig.tsbuildinfo</code>를 루트나 앱 루트에 만들지 않는다.</li>\n<li>stdout/stderr 리다이렉트가 필요하면 <code>$env:TEMP\\codaro\\...</code> 아래를 우선 사용한다.</li>\n<li>보존이 필요 없는 로컬 산출물은 삭제한다. 보존이 꼭 필요하면 레포 밖 <code>$env:TEMP\\codaro\\...</code> 아래를 사용한다.</li>\n<li>Playwright CLI는 레포 루트에서 실행하지 않는다. 브라우저 산출물이 필요하면 임시 작업 폴더나 <code>$env:TEMP\\codaro\\...</code>를 사용한다.</li>\n<li>백그라운드 프로세스를 띄운 작업은 종료 전 반드시 프로세스를 정리하고, 루트에 <code>*.log</code>와 <code>.playwright-cli/</code>가 남지 않았는지 확인한다.</li>\n</ul>\n<h1>주요 명령</h1>\n<pre><code class=\"language-bash\">uv run codaro\nuv run codaro path.py\nuv run codaro export path.py --format codaro\nuv run codaro export path.py --format ipynb\nuv run python -X utf8 tests/run.py preflight\nuv run python -X utf8 tests/run.py list\n</code></pre>\n<h1>패키지 설치</h1>\n<ul>\n<li>사용자가 에디터에서 라이브러리를 설치해도 내부 경로는 <code>/api/packages/install</code> 또는 세션 패키지 capability를 거쳐 <code>uv pip ... --python .venv</code>로 실행한다.</li>\n<li>AI 교사 도구도 같은 경로를 사용한다. <code>packages-check</code>로 누락 여부를 확인한 뒤 <code>packages-install</code>을 호출한다.</li>\n<li>plain package가 이미 <code>.venv</code>에 설치되어 있으면 <code>uv pip install</code>을 다시 실행하지 않고 즉시 성공으로 보고한다. version specifier나 extras가 있으면 정확성을 위해 <code>uv</code> 경로를 탄다.</li>\n<li>설치 결과는 <code>installer: uv</code>, <code>environment: project .venv</code>, <code>durationMs</code>, <code>skipped</code>를 포함한다. 에디터와 teacher trace는 이 값을 사용해 &quot;uv로 준비 중/완료/이미 준비됨&quot;을 표시한다.</li>\n<li>설치 timeout은 긴 wheel 준비를 고려해 <code>INSTALL_TIMEOUT_SECONDS</code> 상수로 관리한다. timeout 변경은 <code>tests/testSystem.py</code>에서 함께 고정한다.</li>\n<li>직접 <code>pip install</code> 명령을 문서, 테스트, UI 안내에 추가하지 않는다.</li>\n</ul>\n<h1>프론트 표면 실행</h1>\n<ul>\n<li><code>editor/</code> — 실제 제품 UI 표면. React + shadcn/ui 기준.</li>\n<li><code>landing/</code> — GitHub Pages 문서와 블로그 표면.</li>\n<li>폐기된 Svelte 편집기는 현재 저장소의 실행 표면으로 보지 않는다.</li>\n</ul>\n<p>제품 프론트를 반복 개발할 때는 <code>editor/</code>를 기준으로 본다.</p>\n",
  "text": "실행 환경 규칙 PowerShell 메인 가상환경은 프로젝트 루트 를 사용한다. WSL에서는 루트 를 공유하지 않는다. WSL 전용 가상환경이 필요하면 을 사용한다. Python 3.12 이상과 를 기준으로 한다. Node.js는 문서 표면과 제품 표면 작업에 필요하다. 실행 인코딩 규칙 Python 실행은 기본적으로 형태를 사용한다. PowerShell에서 인코딩이 의심되면 실행 전에 아래를 적용한다. 파일 읽기/쓰기 명령은 가능한 한 UTF 8을 명시한다. 모든 텍스트 파일은 UTF 8을 기본으로 유지한다. 로컬 산출물 위생 프로젝트 루트는 임시 로그/스크린샷/캐시/세션 파일을 쓰는 장소가 아니다. 백그라운드 서버를 띄울 때 , , , , 를 루트나 앱 루트에 만들지 않는다. stdout/stderr 리다이렉트가 필요하면 아래를 우선 사용한다. 보존이 필요 없는 로컬 산출물은 삭제한다. 보존이 꼭 필요하면 레포 밖 아래를 사용한다. Playwright CLI는 레포 루트에서 실행하지 않는다. 브라우저 산출물이 필요하면 임시 작업 폴더나 를 사용한다. 백그라운드 프로세스를 띄운 작업은 종료 전 반드시 프로세스를 정리하고, 루트에 와 가 남지 않았는지 확인한다. 주요 명령 패키지 설치 사용자가 에디터에서 라이브러리를 설치해도 내부 경로는 또는 세션 패키지 capability를 거쳐 로 실행한다. AI 교사 도구도 같은 경로를 사용한다. 로 누락 여부를 확인한 뒤 을 호출한다. plain package가 이미 에 설치되어 있으면 을 다시 실행하지 않고 즉시 성공으로 보고한다. version specifier나 extras가 있으면 정확성을 위해 경로를 탄다. 설치 결과는 , , , 를 포함한다. 에디터와 teacher trace는 이 값을 사용해 \"uv로 준비 중/완료/이미 준비됨\"을 표시한다. 설치 timeout은 긴 wheel 준비를 고려해 상수로 관리한다. timeout 변경은 에서 함께 고정한다. 직접 명령을 문서, 테스트, UI 안내에 추가하지 않는다. 프론트 표면 실행 — 실제 제품 UI 표면. React + shadcn/ui 기준. — GitHub Pages 문서와 블로그 표면. 폐기된 Svelte 편집기는 현재 저장소의 실행 표면으로 보지 않는다. 제품 프론트를 반복 개발할 때는 를 기준으로 본다."
};
