export const pageContent = {
  "html": "<h1>Curriculum OS</h1>\n<p>학습 커리큘럼을 단순 카테고리 트리에서 <strong>outcome 그래프</strong>로 끌어올린 레이어다. 사용자/teacher가 도메인(목적)을 지정하면 prerequisite을 따라 레슨 순서를 짜고, 커버되지 않는 outcome은 gap으로 리포트한다. Skill OS가 &quot;도구 선택&quot;이라면, Curriculum OS는 &quot;학습 경로 합성&quot;이다.</p>\n<h2>5계층 구조 (Phase 1~3 완성형)</h2>\n<pre><code>taxonomy (SSOT YAML — outcomes/domains/lessonOutcomes/sectionOutcomes/lessonRole)\n  ↓\nlessonGraph (outcome 매핑 + prerequisite DAG + section/role 메타)\n  ↓\nengine (composer + reviewScheduler + outcomeCredit + outcomeMastery + analyticsTimeline + learnerStateBridge)\n  ↓\n{REST API, teacher tools}\n  ↓\nUI surfaces (MasterPlanPanel + MasteryPanel + TodayReviewsCard + AnalyticsPanel)\n</code></pre>\n<h2>SSOT — <code>curricula/python/_taxonomy.yml</code></h2>\n<p>세 가지 vocabulary를 한 파일에 둔다.</p>\n<ul>\n<li><strong>outcomes</strong>: 학생이 한 레슨을 마치고 얻는 단위 능력. ID는 <code>&lt;도메인 prefix&gt;.&lt;능력명&gt;</code> lowerCamelCase. 예: <code>python.variables</code>, <code>pandas.aggregate</code>, <code>automation.browser.session</code>.</li>\n<li><strong>domains</strong>: 학습자가 실제로 달성하려는 실무 목표. <code>targetOutcomes</code> 배열이 도메인을 충족하기 위해 필요한 outcome 집합.</li>\n<li><strong>lessonOutcomes</strong>: <code>&lt;category&gt;/&lt;contentId&gt;</code> 키별 backfill. 레슨 YAML 메타에 <code>outcomes</code>/<code>prerequisites</code>가 있으면 그 쪽이 우선, 없으면 여기 값으로 채운다.</li>\n<li><strong>sectionOutcomes</strong> (Phase 2b): 같은 항목 안의 dict — <code>{sectionId: [outcome, ...]}</code>. 다중 outcome lesson 에서 어느 섹션이 어느 outcome 을 검증하는지 매핑한다. 미지정 시 lesson outcomes 전체로 fallback.</li>\n<li><strong>lessonRole</strong> (Phase 2d): <code>concept | practice | project</code>. project 는 deliverable-driven plan 합성 시 우측 칼럼에 배치되는 마지막 lesson.</li>\n</ul>\n<p>새 레슨을 만들 때 권장 순서:</p>\n<ol>\n<li>필요한 outcome/domain이 없으면 <code>_taxonomy.yml</code>에 먼저 추가</li>\n<li>레슨 YAML <code>meta</code>에 <code>outcomes</code>/<code>prerequisites</code>/<code>estimatedMinutes</code> 적기 (권장) 또는 <code>lessonOutcomes</code>에 등록</li>\n<li><code>tests/curriculum/testCurriculumOs.py</code>가 그래프 무결성을 검증</li>\n</ol>\n<h2>planComposer 알고리즘</h2>\n<p>결정적(deterministic). 같은 입력이면 같은 plan을 낸다.</p>\n<ol>\n<li><strong>목표 outcome 해석</strong> — <code>goal.domain.targetOutcomes ∪ goal.outcomes</code></li>\n<li><strong>projectIntent 매칭</strong> (Phase 2d) — 한국어/영어 키워드를 통해 카테고리 boost, 매칭된 project lesson 들의 outcomes 도 target 에 추가.</li>\n<li><strong>skipMasteredOutcomes 필터</strong> (Phase 1) — <code>MASTERY_THRESHOLD</code> 이상 outcome 은 target/expansion 모두 차단.</li>\n<li><strong>Backward expansion</strong> — 각 target outcome에 대해 best lesson을 선택하고, 그 레슨의 prerequisite outcomes를 큐에 다시 넣는 BFS. 카테고리 우선순위 + sortKey 휴리스틱.</li>\n<li><strong>project lesson 강제 포함</strong> (Phase 2d) — projectIntent 매칭 시 backward expansion 이 입문 lesson 으로 같은 outcome 을 cover 했더라도 deliverable lesson 까지 도달.</li>\n<li><strong>위상 정렬</strong> — 선택된 레슨 사이의 prerequisite 의존을 그래프로 만들고 Kahn&#39;s algorithm으로 정렬. 사이클이 있으면 sortKey fallback.</li>\n<li><strong>완료 필터</strong> — <code>progressTracker</code>에서 <code>completedAt</code>이 있는 레슨 제외 (옵션).</li>\n<li><strong>deliverableOnly 분리</strong> (Phase 2d) — mastery&gt;=0.6 인 outcome 만으로 구성된 concept lesson 은 <code>droppedSteps</code> 로.</li>\n<li><strong>maxMinutes cropping</strong> (Phase 2a) — prerequisite 순서 유지하며 시간 예산 초과 step 을 <code>droppedSteps</code> 로.</li>\n<li><strong>3단 분리</strong> (Phase 2d) — <code>lessonRole</code> 기준으로 <code>conceptSteps / practiceSteps / projectSteps</code> 합성.</li>\n<li><strong>Gap 리포트</strong> — provider 레슨이 없는 outcome은 <code>PlanGap</code>으로 분리 반환 (<code>gaps</code>), 학습자 mastery 가 낮으면 <code>dynamicGaps</code>.</li>\n</ol>\n<p><code>PRIMARY_CATEGORY_ORDER</code>에 카테고리 우선순위가 명시되어 있다 (기초가 앞).</p>\n<h2>Phase 2b — Outcome Credit &amp; Review</h2>\n<p>체크 통과를 outcome 숙련도 신호로 변환:</p>\n<ul>\n<li><code>outcomeCredit.py</code>: weight 매핑 (hintLevel 0 → 1.0, 1 → 0.7, 2 → 0.5, 3 → 0.3, 그 외 0.2).</li>\n<li><code>progress.py</code>: <code>outcomeCredits</code>, <code>autoValidatedOutcomes</code>, <code>sectionResults</code> 필드. <code>creditCheckPass(category, contentId, sectionId, outcomes, hintLevel)</code> 가 누적.</li>\n<li>3 회 이상 credit + 평균 weight ≥ 0.7 → outcome 자동 검증 (<code>autoValidatedOutcomes</code>).</li>\n<li><code>outcomeMastery.computeMastery</code>: lesson contribution + credit contribution 을 같은 확률 합성에 넣음. 시간 감쇠 half-life 30 일 (Phase 2c).</li>\n</ul>\n<h2>Phase 2c — Review Scheduling</h2>\n<p>SM-2 lite — binary success/lapse 로 다음 review interval 결정:</p>\n<ul>\n<li><code>reviewScheduler.py</code>: <code>ReviewState (interval, ease 1.3~2.5, streak, nextReviewAt, lastResult)</code>, init/updateOnSuccess/updateOnLapse/isDue.</li>\n<li><code>progress.completeMission()</code> 가 lesson 완료 첫 순간 자동으로 <code>ReviewState</code> 생성.</li>\n<li><code>/api/curriculum/reviews</code> / <code>/api/curriculum/reviews/{cat}/{id}</code> 엔드포인트.</li>\n<li>mastery decay: <code>_decayedContribution(base, lastTouched)</code> — half-life 30일, floor 0.25.</li>\n</ul>\n<h2>Phase 3 — Analytics &amp; Bridge</h2>\n<p>학습 패턴을 시간축으로 + 두 mastery source 통합:</p>\n<ul>\n<li><code>analyticsTimeline.py</code>: 일별 <code>DailySnapshot</code> (masteredCount, lessons/sections/credits today, hintHistogram, domainsTouched). JSONL append-only, idempotent per day.</li>\n<li><code>learnerStateBridge.py</code>: <code>outcomeMastery</code> (progress 합성) 와 <code>learnerState</code> (EMA SQLite) 를 0.6/0.4 blend 한 <code>UnifiedOutcomeMastery</code>. 두 저장소는 합치지 않음 — 합성 view 만.</li>\n<li><code>/api/curriculum/analytics</code> / <code>/analytics/summary</code> / <code>/mastery/unified</code> 엔드포인트.</li>\n</ul>\n<h2>API 계약</h2>\n<table>\n<thead>\n<tr>\n<th>Endpoint</th>\n<th>Method</th>\n<th>Payload</th>\n<th>반환</th>\n</tr>\n</thead>\n<tbody><tr>\n<td><code>/api/curriculum/taxonomy</code></td>\n<td>GET</td>\n<td>—</td>\n<td><code>{outcomes, domains}</code></td>\n</tr>\n<tr>\n<td><code>/api/curriculum/master-plan</code></td>\n<td>POST</td>\n<td><code>{domain?, outcomes?, excludeCompleted?, excludeKeys?, skipMasteredOutcomes?, maxMinutes?, projectIntent?, deliverableOnly?}</code></td>\n<td><code>MasterPlan</code> (steps, gaps, droppedSteps, conceptSteps/practiceSteps/projectSteps, projectMatches, totalMinutes, summary)</td>\n</tr>\n<tr>\n<td><code>/api/curriculum/gaps</code></td>\n<td>GET</td>\n<td><code>?domain=</code> (옵션)</td>\n<td><code>{gaps: [{domainId, domainLabel, missing}]}</code></td>\n</tr>\n<tr>\n<td><code>/api/curriculum/mastery</code></td>\n<td>GET</td>\n<td>—</td>\n<td><code>MasteryReport</code> (outcomes, domains, masteredOutcomeCount, totalOutcomeCount)</td>\n</tr>\n<tr>\n<td><code>/api/curriculum/mastery/unified</code></td>\n<td>GET</td>\n<td>—</td>\n<td><code>UnifiedMasteryReport</code> (progress + learnerState blend)</td>\n</tr>\n<tr>\n<td><code>/api/curriculum/outcomes/validate</code></td>\n<td>POST</td>\n<td><code>{outcomeId, validated}</code></td>\n<td>toggle 결과</td>\n</tr>\n<tr>\n<td><code>/api/curriculum/check</code></td>\n<td>POST</td>\n<td><code>{... category?, contentId?, sectionId?}</code></td>\n<td><code>CheckResult</code> + <code>creditedOutcomes</code> + <code>autoValidatedOutcomes</code></td>\n</tr>\n<tr>\n<td><code>/api/curriculum/reviews</code></td>\n<td>GET</td>\n<td>—</td>\n<td><code>{reviews, totalDue}</code></td>\n</tr>\n<tr>\n<td><code>/api/curriculum/reviews/{cat}/{id}</code></td>\n<td>POST</td>\n<td><code>{success}</code></td>\n<td>updated <code>ReviewState</code></td>\n</tr>\n<tr>\n<td><code>/api/curriculum/analytics</code></td>\n<td>GET</td>\n<td><code>?days=30</code></td>\n<td><code>{snapshots, totalSnapshots}</code></td>\n</tr>\n<tr>\n<td><code>/api/curriculum/analytics/summary</code></td>\n<td>GET</td>\n<td>—</td>\n<td>30 일 집계</td>\n</tr>\n</tbody></table>\n<p>unknown domain/outcome ID는 400 + <code>curriculum_unknown_domain</code> / <code>curriculum_unknown_outcome</code>.</p>\n<h2>Teacher 도구</h2>\n<p><a href=\"/codaro/docs/skills/architecture/teacher-tool-loop\">teacher-tool-loop</a>에 등록된 7개:</p>\n<table>\n<thead>\n<tr>\n<th>도구</th>\n<th>용도</th>\n</tr>\n</thead>\n<tbody><tr>\n<td><code>list-curriculum-domains</code></td>\n<td>도메인/outcome 카탈로그</td>\n</tr>\n<tr>\n<td><code>resolve-learning-goal</code></td>\n<td>자연어 → 도메인 후보 랭킹</td>\n</tr>\n<tr>\n<td><code>search-curricula</code></td>\n<td>키워드/카테고리/outcome 기준 레슨 검색</td>\n</tr>\n<tr>\n<td><code>compose-master-plan</code></td>\n<td>도메인/outcome → 순서대로 정렬된 레슨 plan</td>\n</tr>\n<tr>\n<td><code>inspect-curriculum</code></td>\n<td>특정 레슨의 meta/intro 조회</td>\n</tr>\n<tr>\n<td><code>list-curriculum-gaps</code></td>\n<td>도메인별 미충족 outcome 리포트</td>\n</tr>\n<tr>\n<td><code>propose-curriculum-draft</code></td>\n<td>갭을 채울 새 강의 <strong>초안만</strong> 반환 (사람이 검토·작성)</td>\n</tr>\n</tbody></table>\n<blockquote>\n<p><strong>bulk generation 금지</strong>: <code>propose-curriculum-draft</code>는 outline + 메타데이터만 돌려준다. 실제 강의 YAML은 사람이 작성한다 (<a href=\"../../../CLAUDE.md\">CLAUDE.md</a>, <code>feedback_curriculum_no_bulk_generation</code>).</p>\n</blockquote>\n<h2>프론트엔드</h2>\n<p>세 가지 surface 가 등록되어 있다.</p>\n<ul>\n<li><code>SurfaceMode = &quot;plan&quot;</code> — <a href=\"../../../editor/src/components/curriculum/masterPlanPanel.tsx\"><code>masterPlanPanel.tsx</code></a>:<ul>\n<li>도메인 칩 선택 → 자동 plan 합성</li>\n<li>시간 예산 / 이미 익힌 능력 건너뛰기 토글</li>\n<li>projectIntent 텍스트 입력 → deliverableOnly 자동 활성 + 매칭 키워드 chip + 단일↔3단 뷰 토글</li>\n<li>3단 뷰 (<code>TieredPlanBody</code>): 개념(zinc) → 실습(sky) → 프로젝트(emerald) 칼럼</li>\n<li><code>TodayReviewsCard</code>: 오늘 due review 리스트 + 통과/실패 버튼</li>\n<li><code>MasteryPanel</code>: outcome 별 progress + credit count + 자동/수동 검증 토글</li>\n<li>step 클릭 → <code>selectCurriculumCategory/Content</code> → 해당 레슨</li>\n<li>gap &quot;초안 요청&quot; 버튼 → chat surface + propose-curriculum-draft 자동 입력</li>\n</ul>\n</li>\n<li><code>SurfaceMode = &quot;analytics&quot;</code> — <a href=\"../../../editor/src/components/curriculum/analyticsPanel.tsx\"><code>analyticsPanel.tsx</code></a>:<ul>\n<li>mastered/30일 lessons/sections/credits stat 카드 4 개</li>\n<li>SVG mastery 추세 라인 차트</li>\n<li>힌트 분포 바 차트</li>\n<li>최근 활동 도메인 chip</li>\n</ul>\n</li>\n<li>체크 통과 시 <a href=\"../../../editor/src/components/curriculum/checkResultPanel.tsx\"><code>checkResultPanel.tsx</code></a> 가 credit 카드를 그린다 — outcome chip 에 자동 검증된 outcome 은 Trophy 아이콘으로 강조.</li>\n</ul>\n<h2>무결성 게이트</h2>\n<p><code>tests/curriculum/testCurriculumOs.py</code>에 24개 케이스:</p>\n<ul>\n<li>모든 domain.targetOutcomes가 outcome 카탈로그에 존재</li>\n<li>모든 lessonOutcomes의 outcomes/prerequisites가 카탈로그에 존재</li>\n<li>레슨 메타의 outcomes가 taxonomy backfill보다 우선</li>\n<li>합성기가 prerequisite 순서를 지킴</li>\n<li>완료된 레슨이 plan에서 제외됨</li>\n<li>gap이 정확히 리포트됨</li>\n<li>모든 레슨이 plan 그래프에서 보임 (orphan 0개)</li>\n<li>모든 outcome이 어떤 레슨에서 제공됨 (115/115)</li>\n<li>모든 도메인이 비어있지 않은 plan을 만듦 (27/27 도메인, gap 0)</li>\n<li>같은 입력은 같은 plan을 낸다 (결정성 스냅샷)</li>\n<li>API 엔드포인트 통합 테스트 (FastAPI TestClient)</li>\n</ul>\n<p><code>tests/auditCurriculumWeakness.py</code>가 영구 게이트로 등록되어 있다 (<code>tests/run.py gate curriculum-weakness-audit</code>):</p>\n<ul>\n<li>정적 신호: <code>orphanInPlan</code>, <code>noExercise</code> (intro <code>00_*</code> 제외), <code>exerciseWithoutCheck</code>, <code>noHint</code>, <code>shortGoal</code>, <code>sectionIdMissing</code> (Phase 2b)</li>\n<li>카테고리 신호: <code>categoryWithoutProject</code> (Phase 2d, builtins/excel/practical 면제)</li>\n<li>각 신호별 임계치(현재 모두 0)를 넘으면 게이트 실패</li>\n<li>리포트: <code>output/test-runner/curriculum-weakness-audit/curriculum-weakness-report.json</code></li>\n</ul>\n<h2>확장 가이드</h2>\n<p>새 도메인 추가:</p>\n<ol>\n<li><code>_taxonomy.yml</code>의 <code>domains:</code>에 항목 추가 — id, label, description, targetOutcomes</li>\n<li>targetOutcomes에 들어가는 outcome이 모두 존재하는지 확인</li>\n<li>해당 outcome을 제공하는 레슨이 없으면 gap으로 리포트됨 — <code>list-curriculum-gaps</code>로 확인</li>\n<li>갭을 채우려면 새 강의를 직접 작성 (생성기 스크립트 금지)</li>\n</ol>\n<p>새 outcome 추가:</p>\n<ol>\n<li><code>outcomes:</code>에 항목 추가</li>\n<li>어느 레슨이 이 outcome을 제공하는지 <code>lessonOutcomes</code>에 매핑 — 또는 그 레슨 YAML의 <code>meta.outcomes</code>에 등록</li>\n<li>이 outcome을 prerequisite으로 갖는 다른 레슨이 있다면 cycle이 생기지 않게 확인</li>\n</ol>\n<p>캐시는 lazy — 첫 호출 시 로드, 이후 메모리 보존. 핫 리로드는 <code>CurriculumOsCache.invalidate()</code>.</p>\n",
  "text": "Curriculum OS 학습 커리큘럼을 단순 카테고리 트리에서 outcome 그래프 로 끌어올린 레이어다. 사용자/teacher가 도메인(목적)을 지정하면 prerequisite을 따라 레슨 순서를 짜고, 커버되지 않는 outcome은 gap으로 리포트한다. Skill OS가 \"도구 선택\"이라면, Curriculum OS는 \"학습 경로 합성\"이다. 5계층 구조 (Phase 1~3 완성형) SSOT — 세 가지 vocabulary를 한 파일에 둔다. outcomes : 학생이 한 레슨을 마치고 얻는 단위 능력. ID는 lowerCamelCase. 예: , , . domains : 학습자가 실제로 달성하려는 실무 목표. 배열이 도메인을 충족하기 위해 필요한 outcome 집합. lessonOutcomes : 키별 backfill. 레슨 YAML 메타에 / 가 있으면 그 쪽이 우선, 없으면 여기 값으로 채운다. sectionOutcomes (Phase 2b): 같은 항목 안의 dict — . 다중 outcome lesson 에서 어느 섹션이 어느 outcome 을 검증하는지 매핑한다. 미지정 시 lesson outcomes 전체로 fallback. lessonRole (Phase 2d): . project 는 deliverable driven plan 합성 시 우측 칼럼에 배치되는 마지막 lesson. 새 레슨을 만들 때 권장 순서: 1. 필요한 outcome/domain이 없으면 에 먼저 추가 2. 레슨 YAML 에 / / 적기 (권장) 또는 에 등록 3. 가 그래프 무결성을 검증 planComposer 알고리즘 결정적(deterministic). 같은 입력이면 같은 plan을 낸다. 1. 목표 outcome 해석 — 2. projectIntent 매칭 (Phase 2d) — 한국어/영어 키워드를 통해 카테고리 boost, 매칭된 project lesson 들의 outcomes 도 target 에 추가. 3. skipMasteredOutcomes 필터 (Phase 1) — 이상 outcome 은 target/expansion 모두 차단. 4. Backward expansion — 각 target outcome에 대해 best lesson을 선택하고, 그 레슨의 prerequisite outcomes를 큐에 다시 넣는 BFS. 카테고리 우선순위 + sortKey 휴리스틱. 5. project lesson 강제 포함 (Phase 2d) — projectIntent 매칭 시 backward expansion 이 입문 lesson 으로 같은 outcome 을 cover 했더라도 deliverable lesson 까지 도달. 6. 위상 정렬 — 선택된 레슨 사이의 prerequisite 의존을 그래프로 만들고 Kahn's algorithm으로 정렬. 사이클이 있으면 sortKey fallback. 7. 완료 필터 — 에서 이 있는 레슨 제외 (옵션). 8. deliverableOnly 분리 (Phase 2d) — mastery =0.6 인 outcome 만으로 구성된 concept lesson 은 로. 9. maxMinutes cropping (Phase 2a) — prerequisite 순서 유지하며 시간 예산 초과 step 을 로. 10. 3단 분리 (Phase 2d) — 기준으로 합성. 11. Gap 리포트 — provider 레슨이 없는 outcome은 으로 분리 반환 ( ), 학습자 mastery 가 낮으면 . 에 카테고리 우선순위가 명시되어 있다 (기초가 앞). Phase 2b — Outcome Credit & Review 체크 통과를 outcome 숙련도 신호로 변환: : weight 매핑 (hintLevel 0 → 1.0, 1 → 0.7, 2 → 0.5, 3 → 0.3, 그 외 0.2). : , , 필드. 가 누적. 3 회 이상 credit + 평균 weight ≥ 0.7 → outcome 자동 검증 ( ). : lesson contribution + credit contribution 을 같은 확률 합성에 넣음. 시간 감쇠 half life 30 일 (Phase 2c). Phase 2c — Review Scheduling SM 2 lite — binary success/lapse 로 다음 review interval 결정: : , init/updateOnSuccess/updateOnLapse/isDue. 가 lesson 완료 첫 순간 자동으로 생성. / 엔드포인트. mastery decay: — half life 30일, floor 0.25. Phase 3 — Analytics & Bridge 학습 패턴을 시간축으로 + 두 mastery source 통합: : 일별 (masteredCount, lessons/sections/credits today, hintHistogram, domainsTouched). JSONL append only, idempotent per day. : (progress 합성) 와 (EMA SQLite) 를 0.6/0.4 blend 한 . 두 저장소는 합치지 않음 — 합성 view 만. / / 엔드포인트. API 계약 | Endpoint | Method | Payload | 반환 | | | | | | | | GET | — | | | | POST | | (steps, gaps, droppedSteps, conceptSteps/practiceSteps/projectSteps, projectMatches, totalMinutes, summary) | | | GET | (옵션) | | | | GET | — | (outcomes, domains, masteredOutcomeCount, totalOutcomeCount) | | | GET | — | (progress + learnerState blend) | | | POST | | toggle 결과 | | | POST | | + + | | | GET | — | | | | POST | | updated | | | GET | | | | | GET | — | 30 일 집계 | unknown domain/outcome ID는 400 + / . Teacher 도구 에 등록된 7개: | 도구 | 용도 | | | | | | 도메인/outcome 카탈로그 | | | 자연어 → 도메인 후보 랭킹 | | | 키워드/카테고리/outcome 기준 레슨 검색 | | | 도메인/outcome → 순서대로 정렬된 레슨 plan | | | 특정 레슨의 meta/intro 조회 | | | 도메인별 미충족 outcome 리포트 | | | 갭을 채울 새 강의 초안만 반환 (사람이 검토·작성) | bulk generation 금지 : 는 outline + 메타데이터만 돌려준다. 실제 강의 YAML은 사람이 작성한다 ( , ). 프론트엔드 세 가지 surface 가 등록되어 있다. — : 도메인 칩 선택 → 자동 plan 합성 시간 예산 / 이미 익힌 능력 건너뛰기 토글 projectIntent 텍스트 입력 → deliverableOnly 자동 활성 + 매칭 키워드 chip + 단일↔3단 뷰 토글 3단 뷰 ( ): 개념(zinc) → 실습(sky) → 프로젝트(emerald) 칼럼 : 오늘 due review 리스트 + 통과/실패 버튼 : outcome 별 progress + credit count + 자동/수동 검증 토글 step 클릭 → → 해당 레슨 gap \"초안 요청\" 버튼 → chat surface + propose curriculum draft 자동 입력 — : mastered/30일 lessons/sections/credits stat 카드 4 개 SVG mastery 추세 라인 차트 힌트 분포 바 차트 최근 활동 도메인 chip 체크 통과 시 가 credit 카드를 그린다 — outcome chip 에 자동 검증된 outcome 은 Trophy 아이콘으로 강조. 무결성 게이트 에 24개 케이스: 모든 domain.targetOutcomes가 outcome 카탈로그에 존재 모든 lessonOutcomes의 outcomes/prerequisites가 카탈로그에 존재 레슨 메타의 outcomes가 taxonomy backfill보다 우선 합성기가 prerequisite 순서를 지킴 완료된 레슨이 plan에서 제외됨 gap이 정확히 리포트됨 모든 레슨이 plan 그래프에서 보임 (orphan 0개) 모든 outcome이 어떤 레슨에서 제공됨 (115/115) 모든 도메인이 비어있지 않은 plan을 만듦 (27/27 도메인, gap 0) 같은 입력은 같은 plan을 낸다 (결정성 스냅샷) API 엔드포인트 통합 테스트 (FastAPI TestClient) 가 영구 게이트로 등록되어 있다 ( ): 정적 신호: , (intro 제외), , , , (Phase 2b) 카테고리 신호: (Phase 2d, builtins/excel/practical 면제) 각 신호별 임계치(현재 모두 0)를 넘으면 게이트 실패 리포트: 확장 가이드 새 도메인 추가: 1. 의 에 항목 추가 — id, label, description, targetOutcomes 2. targetOutcomes에 들어가는 outcome이 모두 존재하는지 확인 3. 해당 outcome을 제공하는 레슨이 없으면 gap으로 리포트됨 — 로 확인 4. 갭을 채우려면 새 강의를 직접 작성 (생성기 스크립트 금지) 새 outcome 추가: 1. 에 항목 추가 2. 어느 레슨이 이 outcome을 제공하는지 에 매핑 — 또는 그 레슨 YAML의 에 등록 3. 이 outcome을 prerequisite으로 갖는 다른 레슨이 있다면 cycle이 생기지 않게 확인 캐시는 lazy — 첫 호출 시 로드, 이후 메모리 보존. 핫 리로드는 ."
};
