export const pageContent = {
  "html": "<h1>실행 엔진 원칙</h1>\n<ul>\n<li>실행 엔진은 교체 가능한 인터페이스로 설계한다.</li>\n<li>기본 후보:<ul>\n<li><code>LocalEngine</code></li>\n<li><code>SandboxEngine</code></li>\n</ul>\n</li>\n<li>편집기는 <code>execute</code>, <code>interrupt</code>, <code>variables</code>, <code>files</code>, <code>packages</code>, <code>docs</code> 같은 capability를 호출하고, 개별 엔진 구현을 직접 알지 않아야 한다.</li>\n<li>AI teacher와 <code>editor/</code> 제품 표면도 같은 capability surface를 호출한다.</li>\n<li>셀 실행/검증은 장기적으로 <code>cell-call</code> 계약으로 수렴한다. <code>execute-reactive</code>, <code>check-exercise</code>는 하위 호환 및 세부 구현 도구다.</li>\n<li>editor의 직접 실행 버튼도 kernel execute로 바로 들어가지 않는다. <code>editor/src/lib/notebookRuntime.ts</code>가 세션 패키지 목록을 확인하고, 누락 패키지를 세션 <code>packages/install</code> capability의 uv 경로로 준비한 뒤 <code>execute</code> 또는 <code>execute-reactive</code>를 호출한다.</li>\n<li><code>editor-runtime-preflight</code> gate는 이 순서가 <code>packages-check → packages-install → cell-call</code> 의미와 어긋나지 않는지 확인한다.</li>\n<li>kernel 실행 결과를 HTTP, websocket, tool payload로 바꾸는 기준은 <code>src/codaro/kernel/executionPayload.py</code>에 둔다.</li>\n</ul>\n<h2>관련</h2>\n<ul>\n<li>[[local-first-runtime]] — 로컬 기본 실행 정책</li>\n<li>[[reactive-execution]] — 의존 그래프 위에서 동작</li>\n</ul>\n",
  "text": "실행 엔진 원칙 실행 엔진은 교체 가능한 인터페이스로 설계한다. 기본 후보: 편집기는 , , , , , 같은 capability를 호출하고, 개별 엔진 구현을 직접 알지 않아야 한다. AI teacher와 제품 표면도 같은 capability surface를 호출한다. 셀 실행/검증은 장기적으로 계약으로 수렴한다. , 는 하위 호환 및 세부 구현 도구다. editor의 직접 실행 버튼도 kernel execute로 바로 들어가지 않는다. 가 세션 패키지 목록을 확인하고, 누락 패키지를 세션 capability의 uv 경로로 준비한 뒤 또는 를 호출한다. gate는 이 순서가 의미와 어긋나지 않는지 확인한다. kernel 실행 결과를 HTTP, websocket, tool payload로 바꾸는 기준은 에 둔다. 관련 [[local first runtime]] — 로컬 기본 실행 정책 [[reactive execution]] — 의존 그래프 위에서 동작"
};
