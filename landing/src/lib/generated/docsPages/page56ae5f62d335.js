export const pageContent = {
  "html": "<h1>NameError 디버깅 패턴</h1>\n<p><code>NameError: name &#39;...&#39; is not defined</code> 는 이름을 부르기 전에 정의/import 가 안 됐다는\n신호다.</p>\n<h2>1. 같은 셀에서 정의했는지 확인</h2>\n<p>노트북/Codaro 셀은 위에서 아래로 평가된다. 변수를 쓰는 셀보다 정의 셀이 <strong>위쪽</strong> 에\n있어야 한다. 정의 셀을 실행하지 않은 채 사용 셀만 누른 경우가 가장 흔하다.</p>\n<pre><code class=\"language-python\">print(dir())  # 현재 네임스페이스에 무엇이 있는지\n</code></pre>\n<h2>2. 오타·대소문자</h2>\n<p><code>my_var</code> 와 <code>myVar</code> 와 <code>MyVar</code> 는 모두 다른 이름. Python 은 case-sensitive 라\n<code>Print(&#39;hi&#39;)</code> 도 NameError 가 된다. 자주 마주치는 함정: <code>range</code> 를 <code>Range</code>, <code>len</code> 을\n<code>Len</code> 으로.</p>\n<h2>3. import 빠뜨림</h2>\n<p><code>pd.DataFrame(...)</code> 을 부르려면 그 셀(또는 그보다 위 셀)에서 <code>import pandas as pd</code> 가\n실행돼야 한다. 패키지 import 만 따로 모은 setup 셀이 있는지 한 번 더 본다.</p>\n<pre><code class=\"language-python\">import pandas as pd\nimport numpy as np\n</code></pre>\n<p>핵심: NameError 는 거의 항상 <strong>선언 순서</strong> 또는 <strong>철자</strong> 문제. 디버거 대신 <code>dir()</code> /\n스크롤로 빠르게 해결할 수 있다.</p>\n",
  "text": "NameError 디버깅 패턴 는 이름을 부르기 전에 정의/import 가 안 됐다는 신호다. 1. 같은 셀에서 정의했는지 확인 노트북/Codaro 셀은 위에서 아래로 평가된다. 변수를 쓰는 셀보다 정의 셀이 위쪽 에 있어야 한다. 정의 셀을 실행하지 않은 채 사용 셀만 누른 경우가 가장 흔하다. 2. 오타·대소문자 와 와 는 모두 다른 이름. Python 은 case sensitive 라 도 NameError 가 된다. 자주 마주치는 함정: 를 , 을 으로. 3. import 빠뜨림 을 부르려면 그 셀(또는 그보다 위 셀)에서 가 실행돼야 한다. 패키지 import 만 따로 모은 setup 셀이 있는지 한 번 더 본다. 핵심: NameError 는 거의 항상 선언 순서 또는 철자 문제. 디버거 대신 / 스크롤로 빠르게 해결할 수 있다."
};
