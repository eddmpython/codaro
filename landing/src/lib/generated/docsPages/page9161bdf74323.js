export const pageContent = {
  "html": "<h1>로컬 배포 bundle 원칙</h1>\n<ul>\n<li>최종 사용자 배포는 <code>Codaro.exe</code> 하나를 기준으로 한다.</li>\n<li>launcher는 embedded Python runtime과 manifest가 지정한 exact wheel 기반 curated bundle만 설치한다.</li>\n<li>Codaro editor frontend는 기본적으로 <code>codaro</code> wheel 내부 <code>codaro/webBuild</code>에 포함한다. launcher manifest는 <code>editor.source: &quot;backendWheel&quot;</code>을 우선하고, 별도 editor zip은 legacy/internal archive release에서만 쓴다.</li>\n<li>launcher는 index에서 arbitrary latest package를 해석하거나 무제한 <code>pip install</code> 경로를 제품 기본으로 삼지 않는다.</li>\n<li><code>codaro-excel</code> 같은 automation bundle은 Python package, helper runtime, capability probe, bootstrap을 launcher가 관리한다.</li>\n<li>normal <code>vX.Y.Z</code> tag release는 GitHub Release에 exact <code>codaro</code> wheel, <code>release-manifest.json</code>, <code>Codaro.exe</code>, checksum, SPDX SBOM, managed Windows Python runtime archive를 함께 업로드한다.</li>\n<li>같은 게시 릴리즈는 <code>.github/workflows/publish.yaml</code>의 PyPI Trusted Publisher 경로로 <code>codaro</code> wheel/sdist를 PyPI에 올린다. PyPI publisher 값은 project <code>codaro</code>, owner <code>eddmpython</code>, repository <code>codaro</code>, workflow <code>publish.yaml</code>, environment <code>pypi</code>와 일치해야 한다.</li>\n<li>GitHub Pages는 다운로드/문서 표면이다. launcher update는 tag 문자열만 보지 않고 <code>release-manifest.json</code>의 artifact URL과 sha256을 기준으로 한다.</li>\n<li>PyPI는 Python 생태계 검색과 개발자 설치 채널이다. launcher는 PyPI index에서 latest를 해석하지 않고 GitHub Release manifest가 지정한 exact wheel만 설치한다.</li>\n<li>외부 앱과 드라이버 의존성은 별도 경계로 둔다.<ul>\n<li>예: <code>xlwings</code> 기반 Excel app automation은 launcher가 Python 쪽 의존성과 bootstrap을 관리하지만, Microsoft Excel 자체는 사용자가 설치해야 한다.</li>\n</ul>\n</li>\n<li>세부 배포 설계의 source of truth는 <code>launcher/PRD.md</code>, <code>launcher/PACKAGING.md</code>다.</li>\n</ul>\n",
  "text": "로컬 배포 bundle 원칙 최종 사용자 배포는 하나를 기준으로 한다. launcher는 embedded Python runtime과 manifest가 지정한 exact wheel 기반 curated bundle만 설치한다. Codaro editor frontend는 기본적으로 wheel 내부 에 포함한다. launcher manifest는 을 우선하고, 별도 editor zip은 legacy/internal archive release에서만 쓴다. launcher는 index에서 arbitrary latest package를 해석하거나 무제한 경로를 제품 기본으로 삼지 않는다. 같은 automation bundle은 Python package, helper runtime, capability probe, bootstrap을 launcher가 관리한다. normal tag release는 GitHub Release에 exact wheel, , , checksum, SPDX SBOM, managed Windows Python runtime archive를 함께 업로드한다. 같은 게시 릴리즈는 의 PyPI Trusted Publisher 경로로 wheel/sdist를 PyPI에 올린다. PyPI publisher 값은 project , owner , repository , workflow , environment 와 일치해야 한다. GitHub Pages는 다운로드/문서 표면이다. launcher update는 tag 문자열만 보지 않고 의 artifact URL과 sha256을 기준으로 한다. PyPI는 Python 생태계 검색과 개발자 설치 채널이다. launcher는 PyPI index에서 latest를 해석하지 않고 GitHub Release manifest가 지정한 exact wheel만 설치한다. 외부 앱과 드라이버 의존성은 별도 경계로 둔다. 예: 기반 Excel app automation은 launcher가 Python 쪽 의존성과 bootstrap을 관리하지만, Microsoft Excel 자체는 사용자가 설치해야 한다. 세부 배포 설계의 source of truth는 , 다."
};
