export const pageContent = {
  "html": "<h1>Git 및 릴리즈 원칙</h1>\n<p><code>CLAUDE.md</code>의 게이트가 SSOT다. 이 문서는 그 게이트를 풀어 쓴 운영 절차다.</p>\n<h2>AI 흔적 금지</h2>\n<ul>\n<li>커밋, 주석, 문서 어디에도 AI 생성 흔적 문구를 남기지 않는다.</li>\n<li>커밋 메시지의 제목, 본문, trailer 어디에도 <code>AI</code>, <code>GPT</code>, <code>OpenAI</code>, <code>ChatGPT</code>, <code>Codex</code>, <code>Claude</code>, <code>Anthropic</code>, <code>Generated by</code>, <code>Co-Authored-By</code>, <code>Assisted-by</code> 같은 도구명/생성 흔적/기여자 흔적을 남기지 않는다.</li>\n<li><code>Co-Authored-By</code>, <code>Assisted-by</code>, <code>Generated by</code> 류의 contributor trailer는 사람/도구 여부와 무관하게 이 저장소 커밋 메시지에서 사용하지 않는다.</li>\n<li><code>.githooks/commit-msg</code>가 커밋 메시지의 AI 도구명, generated-by 문구, contributor trailer를 차단한다.</li>\n</ul>\n<h2>브랜치</h2>\n<ul>\n<li>로컬 브랜치 생성 금지. 모든 작업은 로컬 <code>main</code>에서 직접 수행하고, 새 로컬 브랜치 생성/전환/비-<code>main</code> push를 하지 않는다.</li>\n<li>저장소 Git hook 경로는 <code>.githooks</code>이며, <code>reference-transaction</code>과 <code>pre-push</code>가 <code>refs/heads/main</code> 외 로컬 브랜치 ref 생성/갱신/푸시를 차단한다. (태그 ref는 허용한다.)</li>\n<li><code>git switch -c</code>, <code>git checkout -b</code>, <code>git branch &lt;name&gt;</code>, <code>git push origin HEAD:&lt;branch&gt;</code>, <code>gh pr checkout</code> 모두 금지.</li>\n<li>외부 contributor PR은 검토/코멘트/머지할 수 있다. 단 로컬 브랜치를 만들지 않는 GitHub 도구, <code>gh pr view/diff</code>, detached/FETCH_HEAD 방식만 사용한다.</li>\n<li>직접 원격 반영은 <code>main -&gt; origin/main</code>만. 기본 원격은 <code>https://github.com/eddmpython/codaro.git</code>. GitHub 원격 작업은 <code>gh</code> 사용을 허용한다.</li>\n<li><code>isolation: &quot;worktree&quot;</code>는 사용하지 않는다.</li>\n</ul>\n<h2>스테이징 규율 — 자기 변경만</h2>\n<p>작업 도중 떠 있던 다른 변경이 본 커밋에 끼면 이력이 한 의도로 안 읽히고 무관 회귀가 숨는다. 그래서 매 커밋:</p>\n<ol>\n<li><strong>세션 시작 스냅샷</strong> — 작업 직전 <code>git status --short</code>. 이미 떠 있던 staged/unstaged/untracked는 <em>본 세션 작업 아님</em>으로 분류한다.</li>\n<li><strong>본인 변경만 명시 add</strong> — <code>git add &lt;만진 파일&gt;</code>로 경로를 직접 적는다. <code>git add -A</code>·<code>git add .</code> 금지. 디렉터리 단위 add는 본 세션이 그 디렉터리 전체를 작성/이동한 경우만.</li>\n<li><strong>staging 격리 점검</strong> — <code>git commit</code> 직전 <code>git diff --cached --name-only</code>가 본인 add 목록과 정확히 일치하는지 확인. 다르면 <code>git restore --staged &lt;파일&gt;</code>로 분리.</li>\n<li><strong>commit 후 사후 점검</strong> — <code>git show --stat HEAD</code>로 실제 들어간 파일 확인. pre-commit hook이 무관 파일을 자동 re-stage했으면 push 전 <code>git reset --soft HEAD~1</code> + <code>git restore --staged</code>로 되돌리고 재커밋.</li>\n</ol>\n<ul>\n<li>한 커밋 = 한 의도. 기능 추가 + 무관 리팩토링 + 자동 생성물을 섞지 않는다. 자동 생성물(landing <code>generated/</code> 등)은 별도 &quot;정리: 생성물 동기화&quot; 커밋으로 분리한다.</li>\n<li>새 기능·버그픽스에는 그 동작을 잠그는 테스트가 같은 커밋 묶음에 동행한다. 의도적 미테스트면 메시지에 사유를 남긴다.</li>\n</ul>\n<h2>커밋 메시지</h2>\n<ul>\n<li>반드시 한글. 변경 성격 + 실제로 무엇을 바꿨는지 함께 담는다.</li>\n<li>주체 중립 또는 사용자 주어. 1인칭 자기참조 금지.</li>\n</ul>\n<h2>Push 게이트 (위에서 아래로 첫 매치)</h2>\n<ol>\n<li><strong>force-push / tag-force / rewrite-push</strong> — 사용자가 &quot;force 해&quot; · &quot;tag 옮겨&quot; · &quot;rewrite push&quot; 같은 단어로 직접 명시한 경우만. 자체 판단 force 금지. main 직접 force push 요청 시 위험을 먼저 경고한 뒤 <code>--force-with-lease</code>로 진행.</li>\n<li><strong>push 거부/보류 트리거</strong> — 발화에 &quot;푸시 하지마&quot; · &quot;push 보류&quot; · &quot;올리지마&quot; · &quot;로컬에만 두자&quot;가 있으면 사이클 끝에도 push 안 함. &quot;커밋 N개 정리 완료, push 보류 중&quot; 한 줄 보고.</li>\n<li><strong>그 외 일반 작업</strong> (bugfix · feature · refactor · loop · 위임 task) — 사이클 끝마다 자동 push. 사이클 = <code>tests/run.py preflight</code>(또는 목표 지정 gate) 통과 + 본 세션 변경 commit 완료 시점. &quot;사이클 N push 완료&quot; 한 줄 보고. 커밋마다 자동 push 금지.</li>\n</ol>\n<p>과거 &quot;push는 사용자 명시 지시 때만&quot; default는 폐기되었다.</p>\n<h2>릴리즈 절차</h2>\n<p>릴리즈는 항상 규칙을 점검하고, <strong>릴리즈마다 릴리즈 메시지(CHANGELOG 섹션)를 작성한다.</strong> Codaro는 PyPI에 배포하지 않으므로(GitHub Releases가 유일 배포 경로) 릴리즈 메시지는 <em>릴리즈 시점</em>에 작성한다.</p>\n<ol>\n<li><strong>버전 결정</strong> — <code>pyproject.toml</code>의 <code>version</code>이 릴리즈 버전이다(<code>uv.lock</code> 동기화). 런처는 <code>launcher/codaro-launcher/Cargo.toml</code>의 <code>version</code>. <code>0.0.x</code> 라인은 다운로드-우선 배포 트레인이다. 릴리즈 워크플로우는 태그가 <code>v{pyproject.version}</code>과 일치하는지 검증한다.</li>\n<li><strong>CHANGELOG.md 섹션 작성</strong> — <a href=\"https://keepachangelog.com/en/1.1.0/\">Keep a Changelog</a> 형식. <code>## {version} - {date}</code> 헤딩 + 한 줄 요약 + <code>### Added/Changed/Fixed/Removed</code> + <code>### Verification</code>(실행한 게이트/검증). 파일 단위로 무엇을 바꿨는지 구체적으로 적는다. 문구 나열이 아니라 실제 변경을 적는다.</li>\n<li><strong>게이트 통과</strong> — <code>tests/run.py preflight</code> + 릴리즈 표면 게이트(<code>install-launcher-smoke</code>, <code>launcher-test</code> 등) 통과.</li>\n<li><strong>태그 + 푸시</strong> — <code>git tag vX.Y.Z &amp;&amp; git push origin vX.Y.Z</code>. 이 태그가 <code>.github/workflows/product-release.yml</code>을 트리거한다.</li>\n<li><strong>릴리즈 본문 = CHANGELOG 섹션</strong> — 워크플로우가 <code>docs/skills/ops/tools/extractChangelogSection.py --version {pyproject.version}</code>로 해당 섹션을 뽑아 GitHub Release 본문으로 쓴다. <strong>CHANGELOG에 그 버전 섹션이 없으면 추출이 비-0으로 실패해 릴리즈가 막힌다</strong> — &quot;릴리즈 메시지 항상 작성&quot;을 기계적으로 강제한다.</li>\n<li><strong>다운로드 검증</strong> — 릴리즈 완료 후 문서의 모든 다운로드 URL이 200으로 떨어지는지 확인하고(아래 <a href=\"#다운로드-링크-계약--항상-최신-릴리즈\">다운로드 링크 계약</a>), 실제 런처 exe를 내려받아 사용자 경로(프로비저닝 → 네이티브 창 → 백엔드)가 동작하는지 확인한다.</li>\n</ol>\n<ul>\n<li>배포는 GitHub Actions trusted publishing 기준을 유지한다.</li>\n<li>릴리즈 자산: 런처 exe(고정 파일명) + sha256 + SPDX SBOM, <code>python-runtime-win-x64.zip</code>(백엔드 의존성 사전 설치) + sha256, exact <code>codaro</code> wheel + sha256, <code>release-manifest.json</code>(wheel/runtime를 핀). 자산 파일명 규칙은 아래 <a href=\"#다운로드-링크-계약--항상-최신-릴리즈\">다운로드 링크 계약</a>을 따른다.</li>\n</ul>\n<h2>다운로드 링크 계약 — 항상 최신 릴리즈</h2>\n<p>README·랜딩·문서의 모든 &quot;다운로드&quot; 버튼/링크는 이 형태만 쓴다:</p>\n<p><code>https://github.com/eddmpython/codaro/releases/latest/download/&lt;자산명&gt;</code></p>\n<ul>\n<li><code>releases/latest</code>는 GitHub가 <strong>draft/prerelease가 아닌 가장 최근 게시 릴리즈</strong>로 자동 해석한다. 따라서 이 URL은 <strong>언제나 최신 릴리즈의 자산을 받는다</strong> — 새 버전을 낼 때마다 버튼을 손댈 필요가 없다.</li>\n<li>단, 끝의 <code>&lt;자산명&gt;</code>이 <strong>그 최신 릴리즈에 실제로 존재하는 정확한 파일명</strong>이어야 한다. 없으면 404 → GitHub가 릴리즈 페이지/오류로 리다이렉트해 &quot;엉뚱한 데로 간다&quot;.</li>\n</ul>\n<h3>자산 파일명은 고정한다</h3>\n<p>&quot;항상 최신 다운로드&quot;가 깨지는 유일한 경로는 <strong>자산 파일명 변경</strong>이다. 그러므로:</p>\n<ul>\n<li>런처 자산명은 한 번 정하면 릴리즈 전반에서 <strong>불변</strong>으로 유지한다. 게시된 자산명은 <code>gh release view --json assets</code>로 확인한다.</li>\n<li>파일명을 바꿔야 하면 순서를 지킨다: <strong>(1)</strong> 워크플로우가 새 이름으로 자산을 빌드/업로드 → <strong>(2)</strong> 그 릴리즈를 게시 → <strong>(3)</strong> 게시 확인 후에야 README·랜딩·문서 링크를 새 이름으로 갱신. 미게시 이름을 먼저 가리키면 즉시 404다. 순서를 뒤집지 않는다.</li>\n</ul>\n<h3>게시 후 다운로드 검증 (필수)</h3>\n<p>릴리즈 게시 직후, 문서에 적힌 모든 다운로드 URL이 실제로 200으로 떨어지는지 확인한다:</p>\n<pre><code class=\"language-bash\">curl -sI -L -o /dev/null -w &quot;%{http_code}  %{url_effective}\\n&quot; \\\n  &quot;https://github.com/eddmpython/codaro/releases/latest/download/&lt;자산명&gt;&quot;\n</code></pre>\n<p><code>200</code> + <code>content-disposition: attachment; filename=&lt;자산명&gt;</code>이면 통과. <code>404</code>면 자산명이 릴리즈와 어긋난 것이니 링크를 게시 자산명으로 맞춘다.</p>\n<h3>사례 (2026-05-31)</h3>\n<p>README가 <code>Codaro.exe</code>(런처 자산을 그 이름으로 바꾸려던 다음 릴리즈용)를 가리켰으나, 게시된 최신 릴리즈는 <code>v0.0.5</code>이고 런처 자산명은 <code>CodaroLauncher.exe</code>였다. 미게시 이름이라 모든 다운로드 링크가 404 → 엉뚱한 페이지로 빠졌다. 링크를 게시 자산 <code>CodaroLauncher.exe</code>로 되돌려 즉시 복구했다. 근본 해결은 런처를 새 이름으로 빌드/업로드하는 릴리즈를 <strong>먼저 게시한 뒤</strong> 링크를 그 이름으로 옮기는 것이다.</p>\n",
  "text": "Git 및 릴리즈 원칙 의 게이트가 SSOT다. 이 문서는 그 게이트를 풀어 쓴 운영 절차다. AI 흔적 금지 커밋, 주석, 문서 어디에도 AI 생성 흔적 문구를 남기지 않는다. 커밋 메시지의 제목, 본문, trailer 어디에도 , , , , , , , , , 같은 도구명/생성 흔적/기여자 흔적을 남기지 않는다. , , 류의 contributor trailer는 사람/도구 여부와 무관하게 이 저장소 커밋 메시지에서 사용하지 않는다. 가 커밋 메시지의 AI 도구명, generated by 문구, contributor trailer를 차단한다. 브랜치 로컬 브랜치 생성 금지. 모든 작업은 로컬 에서 직접 수행하고, 새 로컬 브랜치 생성/전환/비 push를 하지 않는다. 저장소 Git hook 경로는 이며, 과 가 외 로컬 브랜치 ref 생성/갱신/푸시를 차단한다. (태그 ref는 허용한다.) , , , , 모두 금지. 외부 contributor PR은 검토/코멘트/머지할 수 있다. 단 로컬 브랜치를 만들지 않는 GitHub 도구, , detached/FETCH HEAD 방식만 사용한다. 직접 원격 반영은 만. 기본 원격은 . GitHub 원격 작업은 사용을 허용한다. 는 사용하지 않는다. 스테이징 규율 — 자기 변경만 작업 도중 떠 있던 다른 변경이 본 커밋에 끼면 이력이 한 의도로 안 읽히고 무관 회귀가 숨는다. 그래서 매 커밋: 1. 세션 시작 스냅샷 — 작업 직전 . 이미 떠 있던 staged/unstaged/untracked는 본 세션 작업 아님 으로 분류한다. 2. 본인 변경만 명시 add — 로 경로를 직접 적는다. · 금지. 디렉터리 단위 add는 본 세션이 그 디렉터리 전체를 작성/이동한 경우만. 3. staging 격리 점검 — 직전 가 본인 add 목록과 정확히 일치하는지 확인. 다르면 로 분리. 4. commit 후 사후 점검 — 로 실제 들어간 파일 확인. pre commit hook이 무관 파일을 자동 re stage했으면 push 전 + 로 되돌리고 재커밋. 한 커밋 = 한 의도. 기능 추가 + 무관 리팩토링 + 자동 생성물을 섞지 않는다. 자동 생성물(landing 등)은 별도 \"정리: 생성물 동기화\" 커밋으로 분리한다. 새 기능·버그픽스에는 그 동작을 잠그는 테스트가 같은 커밋 묶음에 동행한다. 의도적 미테스트면 메시지에 사유를 남긴다. 커밋 메시지 반드시 한글. 변경 성격 + 실제로 무엇을 바꿨는지 함께 담는다. 주체 중립 또는 사용자 주어. 1인칭 자기참조 금지. Push 게이트 (위에서 아래로 첫 매치) 1. force push / tag force / rewrite push — 사용자가 \"force 해\" · \"tag 옮겨\" · \"rewrite push\" 같은 단어로 직접 명시한 경우만. 자체 판단 force 금지. main 직접 force push 요청 시 위험을 먼저 경고한 뒤 로 진행. 2. push 거부/보류 트리거 — 발화에 \"푸시 하지마\" · \"push 보류\" · \"올리지마\" · \"로컬에만 두자\"가 있으면 사이클 끝에도 push 안 함. \"커밋 N개 정리 완료, push 보류 중\" 한 줄 보고. 3. 그 외 일반 작업 (bugfix · feature · refactor · loop · 위임 task) — 사이클 끝마다 자동 push. 사이클 = (또는 목표 지정 gate) 통과 + 본 세션 변경 commit 완료 시점. \"사이클 N push 완료\" 한 줄 보고. 커밋마다 자동 push 금지. 과거 \"push는 사용자 명시 지시 때만\" default는 폐기되었다. 릴리즈 절차 릴리즈는 항상 규칙을 점검하고, 릴리즈마다 릴리즈 메시지(CHANGELOG 섹션)를 작성한다. Codaro는 PyPI에 배포하지 않으므로(GitHub Releases가 유일 배포 경로) 릴리즈 메시지는 릴리즈 시점 에 작성한다. 1. 버전 결정 — 의 이 릴리즈 버전이다( 동기화). 런처는 의 . 라인은 다운로드 우선 배포 트레인이다. 릴리즈 워크플로우는 태그가 과 일치하는지 검증한다. 2. CHANGELOG.md 섹션 작성 — 형식. 헤딩 + 한 줄 요약 + + (실행한 게이트/검증). 파일 단위로 무엇을 바꿨는지 구체적으로 적는다. 문구 나열이 아니라 실제 변경을 적는다. 3. 게이트 통과 — + 릴리즈 표면 게이트( , 등) 통과. 4. 태그 + 푸시 — . 이 태그가 을 트리거한다. 5. 릴리즈 본문 = CHANGELOG 섹션 — 워크플로우가 로 해당 섹션을 뽑아 GitHub Release 본문으로 쓴다. CHANGELOG에 그 버전 섹션이 없으면 추출이 비 0으로 실패해 릴리즈가 막힌다 — \"릴리즈 메시지 항상 작성\"을 기계적으로 강제한다. 6. 다운로드 검증 — 릴리즈 완료 후 문서의 모든 다운로드 URL이 200으로 떨어지는지 확인하고(아래 ), 실제 런처 exe를 내려받아 사용자 경로(프로비저닝 → 네이티브 창 → 백엔드)가 동작하는지 확인한다. 배포는 GitHub Actions trusted publishing 기준을 유지한다. 릴리즈 자산: 런처 exe(고정 파일명) + sha256 + SPDX SBOM, (백엔드 의존성 사전 설치) + sha256, exact wheel + sha256, (wheel/runtime를 핀). 자산 파일명 규칙은 아래 을 따른다. 다운로드 링크 계약 — 항상 최신 릴리즈 README·랜딩·문서의 모든 \"다운로드\" 버튼/링크는 이 형태만 쓴다: 는 GitHub가 draft/prerelease가 아닌 가장 최근 게시 릴리즈 로 자동 해석한다. 따라서 이 URL은 언제나 최신 릴리즈의 자산을 받는다 — 새 버전을 낼 때마다 버튼을 손댈 필요가 없다. 단, 끝의 이 그 최신 릴리즈에 실제로 존재하는 정확한 파일명 이어야 한다. 없으면 404 → GitHub가 릴리즈 페이지/오류로 리다이렉트해 \"엉뚱한 데로 간다\". 자산 파일명은 고정한다 \"항상 최신 다운로드\"가 깨지는 유일한 경로는 자산 파일명 변경 이다. 그러므로: 런처 자산명은 한 번 정하면 릴리즈 전반에서 불변 으로 유지한다. 게시된 자산명은 로 확인한다. 파일명을 바꿔야 하면 순서를 지킨다: (1) 워크플로우가 새 이름으로 자산을 빌드/업로드 → (2) 그 릴리즈를 게시 → (3) 게시 확인 후에야 README·랜딩·문서 링크를 새 이름으로 갱신. 미게시 이름을 먼저 가리키면 즉시 404다. 순서를 뒤집지 않는다. 게시 후 다운로드 검증 (필수) 릴리즈 게시 직후, 문서에 적힌 모든 다운로드 URL이 실제로 200으로 떨어지는지 확인한다: + 이면 통과. 면 자산명이 릴리즈와 어긋난 것이니 링크를 게시 자산명으로 맞춘다. 사례 (2026 05 31) README가 (런처 자산을 그 이름으로 바꾸려던 다음 릴리즈용)를 가리켰으나, 게시된 최신 릴리즈는 이고 런처 자산명은 였다. 미게시 이름이라 모든 다운로드 링크가 404 → 엉뚱한 페이지로 빠졌다. 링크를 게시 자산 로 되돌려 즉시 복구했다. 근본 해결은 런처를 새 이름으로 빌드/업로드하는 릴리즈를 먼저 게시한 뒤 링크를 그 이름으로 옮기는 것이다."
};
