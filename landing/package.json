{
  "name": "codaro-landing",
  "private": true,
  "version": "0.0.1",
  "type": "module",
  "scripts": {
    "dev": "vite dev",
    "prebuild": "node scripts/syncBrand.js && node scripts/syncPdfWorker.js && node scripts/syncBlogAssets.js prepare && node scripts/generateContent.js",
    "build": "vite build",
    "postbuild": "node scripts/prerenderReact.js && node scripts/postbuild.js",
    "check": "node scripts/syncBrand.js && node scripts/syncPdfWorker.js && node scripts/syncBlogAssets.js prepare && node scripts/generateContent.js && vite build && node scripts/prerenderReact.js && node scripts/postbuild.js",
    "preview": "vite preview"
  },
  "devDependencies": {
    "@vitejs/plugin-react": "^5.2.0",
    "vite": "^7.3.1"
  },
  "dependencies": {
    "fflate": "^0.8.2",
    "gray-matter": "^4.0.3",
    "lucide-react": "^1.16.0",
    "marked": "^16.4.1",
    "pdf-lib": "^1.17.1",
    "pdfjs-dist": "^4.7.76",
    "react": "^19.2.6",
    "react-dom": "^19.2.6",
    "xlsx": "^0.18.5"
  }
}
