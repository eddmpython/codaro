# Initiative `_done` 자리표시자

이 폴더에는 구현, 지정 테스트, 시각 검토, 문서 갱신까지 끝난 workstream만 폴더째 이동한다.

이 README는 빈 디렉터리 계약을 유지하는 자리표시자이며 완료 증거가 아니다. 현재 이동된 workstream은 0개다. 활성 workstream 문서에 `완료`나 `done`을 적는 것으로 대체할 수 없다.
