# Design Foundation `_done` 자리표시자

구현, 지정 gate, desktop/mobile 시각 검토, 문서 갱신, completion evidence와 transition을 모두 통과한 packet만 이 경로로 이동한다.

이 README는 빈 디렉터리 계약을 유지하는 자리표시자이며 완료 증거가 아니다. 현재 이동된 packet은 0개다.
